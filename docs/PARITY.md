<!--
Copyright (c) DCSV. All rights reserved.
-->

# PARITY.md — Cross-Language Parity Tracking

> Template + justification framework for any cross-language additions in D²-WORX.
>
> Backend is **.NET only** (the SvelteKit BFF is the only non-.NET surface). This doc is the cross-language parity template — populated as cross-language components ship.

---

## When Cross-Language Parity Matters

Parity tracking applies when the SAME concept must exist in MULTIPLE languages. Examples:

- A shared abstraction (D2Result, BaseHandler) implemented in both C# and TypeScript
- A wire-format type generated in multiple languages from the same proto
- A test matcher pattern duplicated in xUnit + Vitest
- A naming convention enforced consistently across languages

When a concept lives in ONE language only, parity isn't a concern — it's exclusive.

---

## "Why Exclusive?" Framework

Before adding a cross-language component, justify why it must be cross-language. Single-language exclusivity is the default; parity is the exception.

For each entry in the parity table, the **Why exclusive?** column documents the reason if a counterpart in another language is intentionally absent. Acceptable reasons:

| Reason                                 | Example                                                                                                                                                                 |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Language has built-in equivalent**   | `Microsoft.Extensions.DependencyInjection` is built into .NET — no D2-specific DI container needed                                                                      |
| **Single-language consumer surface**   | If only the SvelteKit BFF needs a primitive, it lives in TS only                                                                                                        |
| **Different ergonomic ceiling**        | Some patterns (e.g., reflection-driven `[RedactData]`) work better in one language than the other; the other can use a different approach (per-handler `RedactionSpec`) |
| **Single-language runtime constraint** | Python-only ML library, JVM-only compliance toolkit                                                                                                                     |
| **Frozen — out of parity scope**       | Component is OUT OF SCOPE for cross-language parity (no porting expected)                                                                                               |

Unacceptable "exclusive" reasons (= force parity instead):

- "Nobody asked for the other one yet" — if both languages have the same problem, both should have the same solution
- "Implementation cost" — parity reduces long-term cost; exclusivity adds it
- "We already have one, adding another would be inconsistent" — circular reasoning; the inconsistency IS the parity gap

---

## Parity Table Template

Use this table when (re)introducing cross-language components.

| Concern            | .NET                | SvelteKit                                                                           | Other languages | Why exclusive? (per cell)                                                                          |
| ------------------ | ------------------- | ----------------------------------------------------------------------------------- | --------------- | -------------------------------------------------------------------------------------------------- |
| (example) D2Result | `D2.Shared.Result`  | (SvelteKit doesn't have BaseHandler-style handlers; result type imported as needed) | —               | SvelteKit consumes via REST proxy; no per-handler invocation pattern                               |
| (example) Handler  | `D2.Shared.Handler` | —                                                                                   | —               | SvelteKit BFF doesn't have `BaseHandler`-style handlers (load functions instead); no parity needed |

Cross-language additions land here as they ship.

| Concern                                                                                                                                                                                                                                                                                                                                                                                                                                       | .NET                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | SvelteKit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Other languages | Why exclusive? (per cell)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| RFC 7807 ProblemDetails wire catalog (`TYPE_URI_PREFIX` + `CONTENT_TYPE` + 5 `EXTENSION_*` keys + 10 `TITLE_*` per-HTTP-status titles + `TitleFor` switch)                                                                                                                                                                                                                                                                                    | `D2.Shared.ProblemDetails.D2ProblemDetailsKeys` static class — codegen-emitted into `D2.Shared.ProblemDetails.Abstractions` by `D2.Shared.ProblemDetails.SourceGen` from `contracts/problem-details/problem-details.spec.json`. Consumed by both `D2.Shared.Auth.Http` (path A — auth middleware `D2ProblemDetailsExtensions.ToProblemDetails`) AND `D2.Shared.AspNetCore` (path B — `D2ProblemDetailsCustomizer` over ASP.NET `IProblemDetailsService`)                                                                                                                                                                                                                                                                       | `@d2/headers` package — `PROBLEM_TYPE_URI_PREFIX` + `PROBLEM_DETAILS_CONTENT_TYPE` + `ProblemDetailsExtensionKeys` + `ProblemDetailsTitles` + `defaultTitleForStatus` emitted by `tools/ts-codegen/src/problem-details-emit.ts` from the same spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/problem-details.parity.test.ts` — drift surfaces at next CI run. Both .NET emit paths (auth middleware + ASP.NET Customizer) produce byte-identical Shape A bodies for identical D2Result inputs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Generic D2Result error-codes catalog (15-entry `NOT_FOUND` / `FORBIDDEN` / `VALIDATION_FAILED` / `PARTIAL_SUCCESS` / `IDEMPOTENCY_IN_FLIGHT` / `COULD_NOT_BE_SERIALIZED` / `COULD_NOT_BE_DESERIALIZED` / `SOME_FOUND` / `SERVICE_UNAVAILABLE` / `RATE_LIMITED` / `PAYLOAD_TOO_LARGE` / `CONFLICT` / `UNHANDLED_EXCEPTION` / `UNAUTHORIZED` / `CANCELED` + `AllCodes` enumeration + `GetHttpStatus` switch)                                    | `D2.Shared.Result.ErrorCodes` static class — codegen-emitted into `D2.Shared.Result` by `D2.Shared.Result.ErrorCodes.SourceGen` from `contracts/error-codes/error-codes.spec.json`. Consumed by `D2.Shared.Result.D2Result` factories + per-code booleans (`IsNotFound` / `IsConflict` / etc.) + every transport binding's error-code emission path                                                                                                                                                                                                                                                                                                                                                                            | `@d2/result` package — `ErrorCodes` + `ALL_ERROR_CODES` + `getErrorHttpStatus` emitted by `tools/ts-codegen/src/error-codes-emit.ts` from the same spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/error-codes.parity.test.ts` — drift surfaces at next CI run. Auth-specific `AUTH_*` codes live in a sibling spec (`contracts/auth-error-codes/`) because they carry additional fields (`factoryName` / `userMessageKey` / `category`); see the `AuthErrorCodes` row when added                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| TKMessage wire shape (`KEY = "key"` + `PARAMS = "params"` JSON property-name constants governing the `{key, params?}` envelope shipped across .NET ↔ TS for every translatable message)                                                                                                                                                                                                                                                       | `D2.Shared.I18n.TkMessageWireShape` static class — codegen-emitted into `D2.Shared.I18n.Abstractions` by `D2.Shared.WireShapes.SourceGen` (multi-target dispatch by assembly name) from `contracts/tk-message/tk-message.spec.json`. Referenced by `TKMessageJsonConverter.Read`/`Write` for both serialization paths so JSON property names come from one source of truth                                                                                                                                                                                                                                                                                                                                                     | `@d2/result` package — `TkMessageWireShape` `as const` emitted by `tools/ts-codegen/src/wire-shape-emit.ts` from the same spec; referenced by the `tk()` builder / `TKMessage` interface JSDoc / `render-messages.ts` boundary helper                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/tk-message.parity.test.ts` — drift surfaces at next CI run. Round-trip parity fixtures (`round-trip-no-params` / `round-trip-with-params` / `round-trip-with-multiple-params`) capture the byte shape produced by `TKMessageJsonConverter` so the TS-side parser reads what the .NET-side writer emits, end-to-end. `D2Result.messages` ships as `IReadOnlyList<TKMessage>` on both .NET and TS sides — the spec-driven `{key, params?}` envelope is the single source of truth for the wire shape.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| InputError wire shape (`FIELD = "field"` + `ERRORS = "errors"` JSON property-name constants governing the `{field, errors: TKMessage[]}` envelope shipped across .NET ↔ TS for every per-field validation error)                                                                                                                                                                                                                              | `D2.Shared.Result.InputErrorWireShape` static class — codegen-emitted into `D2.Shared.Result` by `D2.Shared.WireShapes.SourceGen` from `contracts/input-error/input-error.spec.json`. Referenced by `InputError`'s `[JsonPropertyName(InputErrorWireShape.FIELD/ERRORS)]` attributes so the .NET serializer ships byte-equal property names under ANY `JsonSerializerOptions` (not just the camelCase-policy SR_Web preset)                                                                                                                                                                                                                                                                                                    | `@d2/result` package — `InputErrorWireShape` `as const` emitted by `tools/ts-codegen/src/wire-shape-emit.ts` from the same spec; referenced by the `inputError()` builder / `InputError` interface JSDoc / `render-messages.ts` boundary helper / `form-helpers.ts` Superforms bridge                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/input-error.parity.test.ts` — drift surfaces at next CI run. Round-trip parity fixtures (`round-trip-single-error` / `round-trip-multiple-errors` / `round-trip-dot-notation-field`) include nested TKMessage shapes proving the wire composition holds. `form-helpers.ts` reads the object-shaped `{field, errors}` envelope directly via the spec catalog, regression-pinned by Vitest.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D2Result Shape B wire envelope (7 JSON property-name constants — `SUCCESS = "success"` / `DATA = "data"` / `MESSAGES = "messages"` / `INPUT_ERRORS = "inputErrors"` / `ERROR_CODE = "errorCode"` / `TRACE_ID = "traceId"` / `STATUS_CODE = "statusCode"` — the BFF gateway response shape every frontend reads)                                                                                                                               | `D2.Shared.Result.D2ResultEnvelopeFieldNames` static class — codegen-emitted into `D2.Shared.Result` by `D2.Shared.Result.Envelope.SourceGen` from `contracts/d2result-envelope/d2result-envelope.spec.json`. Referenced by `[JsonPropertyName(D2ResultEnvelopeFieldNames.*)]` attributes on every wire-emitted `D2Result` / `D2Result<TData>` property — the envelope keys ship as camelCase unconditionally, independent of the calling endpoint's `JsonSerializerOptions.PropertyNamingPolicy`. The 17 `Is*` discriminators on `D2Result.Booleans.cs` carry `[JsonIgnore]` so they never leak onto the wire.                                                                                                                | `@d2/result` package — `D2ResultEnvelopeFieldNames` `as const` emitted by `tools/ts-codegen/src/d2result-envelope-emit.ts` from the same spec; consumed by `server/web/src/lib/shared/rest/gateway-response.ts` (the BFF gateway parser indexes by the spec constants instead of hand-rolled string literals; a `NormalizedBody`-vs-catalog compile-time pin fires if either side drifts)                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/d2result-envelope.parity.test.ts` — drift surfaces at next CI run. Round-trip parity fixtures (`round-trip-ok` / `round-trip-ok-with-data` / `round-trip-not-found` / `round-trip-validation-failed` / `round-trip-with-trace-id`) capture the byte shape produced by `JsonSerializer.Serialize(D2Result)` so the TS-side parser reads exactly what the .NET-side serializer emits. Pinned: the wire ONLY carries the 7 spec fields — `D2Result.Booleans` discriminators are `[JsonIgnore]`'d (structural guard via `D2ResultJsonShapeTests.Serialize_DoesNotLeakIsBooleanHelpers` + `Serialize_WireKeysSubsetOfCatalog`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| gRPC trailers catalog (`ERROR_CODE = "d2_error_code"` + `MESSAGES = "d2_messages"` + `TRACE_ID = "traceId"` trailer wire keys emitted on auth-pipeline `RpcException` failures)                                                                                                                                                                                                                                                               | `D2.Shared.Auth.Grpc.Status.D2GrpcTrailers` static class — codegen-emitted into `D2.Shared.Auth.Grpc` by `D2.Shared.Grpc.Trailers.SourceGen` from `contracts/grpc-trailers/grpc-trailers.spec.json`. Consumed by `D2RpcStatusExtensions.ToRpcException`. The camelCase `traceId` value matches the HTTP ProblemDetails extension key `traceId` — single mental model for operators. gRPC HTTP/2 trailer names are case-insensitive per the spec; the catalog pins camelCase so consumers comparing the trailer value against the HTTP extension key share one string literal across transports.                                                                                                                                | `@d2/grpc-client` package — `D2GrpcTrailers` `as const` emitted by `tools/ts-codegen/src/grpc-trailers-emit.ts` from the same spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/grpc-trailers.parity.test.ts`. Regression-pinned: `TRACE_ID` MUST be `"traceId"` (camelCase) — assertion explicit in both .NET emitter unit test + TS parity test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| OTel messaging activity-tags catalog (`MESSAGING_SYSTEM` / `MESSAGING_DESTINATION_NAME` / `MESSAGING_RABBITMQ_ROUTING_KEY` / `MESSAGING_RABBITMQ_DELIVERY_TAG` / `MESSAGING_RABBITMQ_REDELIVERED` / `MESSAGING_MESSAGE_ID` / `MESSAGING_OPERATION_TYPE` / `D2_MESSAGE_TYPE` / `D2_ENCRYPTION_KID` — ~10 closed-catalog OTel sem-conv attribute names)                                                                                         | `D2.Shared.Messaging.RabbitMq.MessagingActivityTags` static class — codegen-emitted into `D2.Shared.Messaging.RabbitMq` by `D2.Shared.OtelMessagingTags.SourceGen` from `contracts/otel-messaging-tags/otel-messaging-tags.spec.json`. Consumed by `RabbitMqMessageBus` (publisher spans) AND `SubscriberChannel` (consumer spans). Both sides reference the same `MESSAGING_OPERATION_TYPE` constant, so publisher and consumer spans emit byte-identical OTel canonical attribute names — drift between the two sides is structurally impossible.                                                                                                                                                                            | `@d2/telemetry` package — `MessagingActivityTags` `as const` emitted by `tools/ts-codegen/src/otel-messaging-tags-emit.ts` from the same spec; consumed by TS-side messaging instrumentation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/otel-messaging-tags.parity.test.ts`. Regression-pinned: `MESSAGING_OPERATION_TYPE` MUST be `"messaging.operation.type"` (OTel canonical), NOT `"messaging.operation"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| EncryptionDomains catalog (`AUDIT` / `NOTIFICATIONS` / `COURIER` + `PLAINTEXT` sentinel — closed-enum keyring-domain identifiers)                                                                                                                                                                                                                                                                                                             | `D2.Shared.Encryption.EncryptionDomains` static class — codegen-emitted into `D2.Shared.Encryption` by `D2.Shared.EncryptionDomains.SourceGen` from `contracts/encryption-domains/encryption-domains.spec.json`. Spec-driven so a typo can't silently route a message to a non-existent keyring. Cross-spec-validated by `mq-messages.spec.json:encryption` field (the `MqMessages.SourceGen` validates each entry's encryption value against the closed catalog at codegen time).                                                                                                                                                                                                                                             | `@d2/encryption-abstractions` package — `EncryptionDomains` `as const` emitted by `tools/ts-codegen/src/encryption-domains-emit.ts` from the same spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/encryption-domains.parity.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| DLQ failure-metadata wire shape — TWO sub-catalogs: (a) 6 JSON property names (`cause` / `errorCode` / `detail` / `attemptCount` / `traceId` / `nackedBy`); (b) 5 closed-enum cause strings (`HANDLER_RESULT_FAILURE` / `HANDLER_EXCEPTION` / `DECRYPT_FAILURE` / `DESERIALIZE_FAILURE` / `RETRIES_EXHAUSTED`)                                                                                                                                | Multi-target dispatch: `D2.Shared.Messaging.DlqFailureMetadataFields` (Fields catalog) codegen-emitted into `D2.Shared.Messaging.Abstractions`; `D2.Shared.Messaging.RabbitMq.Subscribing.DlqFailureCauses` (Causes catalog) codegen-emitted into `D2.Shared.Messaging.RabbitMq`. Both emitted by `D2.Shared.Messaging.DlqMetadata.SourceGen` from `contracts/dlq-failure-metadata/dlq-failure-metadata.spec.json`. `DlqFailureMetadata` uses `[JsonPropertyName(DlqFailureMetadataFields.*)]` referencing the codegen constants — eliminates implicit `MessagingJsonOptions` camelCase reliance.                                                                                                                              | `@d2/messaging-abstractions` package — `DlqFailureMetadataFields` + `DlqFailureCauses` emitted by `tools/ts-codegen/src/dlq-failure-metadata-emit.ts` from the same spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/dlq-failure-metadata.parity.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Encryption-frame binary-layout catalog (per-field byte offsets + lengths for the on-wire frame `[version:1][kid_len:1][kid:UTF-8 N][nonce:12][ciphertext+tag:M]` + frame-level constraints — `CURRENT_VERSION` / `*_OFFSET` / `*_LENGTH` / `CONSTRAINT_MIN_KID_LENGTH` / `CONSTRAINT_MAX_KID_LENGTH` / `CONSTRAINT_NONCE_LENGTH` / `CONSTRAINT_TAG_LENGTH` / `CONSTRAINT_MIN_FRAME_SIZE`)                                                     | `D2.Shared.Encryption.EncryptionFrameLayout` static class — codegen-emitted into `D2.Shared.Encryption` by `D2.Shared.EncryptionFrame.SourceGen` from `contracts/encryption-frame/encryption-frame.spec.json`. The .NET encoder and the TS-side `@d2/encryption-abstractions` decoder reference the SAME catalog so the binary layout cannot drift across language boundaries.                                                                                                                                                                                                                                                                                                                                                 | `@d2/encryption-abstractions` package — `EncryptionFrame` `as const` emitted by `tools/ts-codegen/src/encryption-frame-emit.ts` from the same spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/encryption-frame.parity.test.ts`. Regression-pinned: `CURRENT_VERSION` MUST stay `1` (bumping is wire-breaking); `CONSTRAINT_NONCE_LENGTH` / `CONSTRAINT_TAG_LENGTH` MUST stay at the GCM-spec values 12 / 16                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Geo record shapes — seven catalog record types (`Country` / `Subdivision` / `Currency` / `Language` / `Locale` / `Timezone` / `GeopoliticalEntity`) with identical field names, nullability semantics, and FK-nav structure on both sides. `GeoCatalog` constants (`CatalogVersion` string + `CatalogPublishedAt` timestamp) sourced from spec metadata.                                                                                      | `D2.Shared.Geo.Abstractions` — record shapes + `GeoCatalog` constants codegen-emitted by `D2.Shared.Geo.SourceGen` from seven `contracts/geo/*.spec.json` specs into `geo/abstractions/Generated/`. Field names use PascalCase; FK-code properties carry `*Code` suffix; nav-list properties are wired by the two-pass populate pattern in `geo/default/`. `DateTimeOffset CatalogPublishedAt` (NOT NodaTime `Instant` — keeps this assembly NodaTime-free).                                                                                                                                                                                                                                                                   | `@d2/geo-abstractions` — same record interfaces + `GeoCatalog` `as const` codegen-emitted by `tools/ts-codegen/src/geo-emitter/` from the same seven specs into `geo/abstractions/src/generated/`. Field names use camelCase; FK-code properties carry `Code`-suffix branded types; nav properties wired by the two-pass coordinator in `@d2/geo-default`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/geo-records.parity.test.ts` (fixture emitted by `D2.Shared.Tests.Integration.ContractFixtures.GeoRecordsFixtureEmitter`) + `geo-catalog.parity.test.ts`. Structural drift between the seven record shapes on either side is caught at the next CI run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Geo enum + wrapper-struct catalogs — four `*Code` closed-set enums (`CountryCode` / `CurrencyCode` / `LanguageCode` / `GeopoliticalEntityCode`) + three `*Code` wrapper-struct types (`SubdivisionCode` / `LocaleCode` / `TimezoneCode`) + five fixed-vocabulary enums (`WritingDirection` / `DateFormatPattern` / `MeasurementSystem` / `CurrencyAcceptanceLevel` / `GeoDayOfWeek`) + one integer-wire enum (`GeopoliticalEntityType`).      | `D2.Shared.Geo.Abstractions` — enums + wrapper structs codegen-emitted by `D2.Shared.Geo.SourceGen`. String-wire enums serialize via `JsonStringEnumConverter`; `GeopoliticalEntityType` uses integer backing. Wrapper structs expose `TryParse` / `ToString` with lenient-parser contract (case-insensitive, trim).                                                                                                                                                                                                                                                                                                                                                                                                           | `@d2/geo-abstractions` — `as const` string-valued objects (ISO codes) + branded `string` types for wrapper structs + integer `as const` for `GeopoliticalEntityType`; Zod refinement schemas for every catalog type; `ALL_*_SET` `ReadonlySet<string>` for O(1) membership checks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/geo-enums.parity.test.ts` (fixture emitted by `GeoEnumsFixtureEmitter`) + `geo-wrapper-structs.parity.test.ts` (fixture emitted by `GeoWrapperStructsFixtureEmitter`). Per-VALUE byte-equivalence asserted for every enum member and every known wrapper-struct code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Geo name-resolver + normalizer + Levenshtein comparer — `IGeoNameResolver` / `NameNormalizer` / `LevenshteinComparer`. Four-pass fail-closed cascade: exact → prefix → fuzzy → ambiguity-sentinel. Byte-identical normalization pipeline and edit-distance function on both runtimes.                                                                                                                                                         | `D2.Shared.Geo.Abstractions` — `IGeoNameResolver` interface; `NameNormalizer` + `LevenshteinComparer` static classes (pure functions). `D2.Shared.Geo.Default` — `DefaultGeoNameResolver` implementation with cache-aside + ambiguity-sentinel pattern.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `@d2/geo-abstractions` — `IGeoNameResolver` TS interface; `nameNormalizer` + `levenshteinComparer` pure-function modules. `@d2/geo-default` — `DefaultGeoNameResolver` implementation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —               | Cross-language parity enforced by `server/shared/typescript/contract-tests/tests/geo-name-resolver.parity.test.ts` (fixture `contracts/geo/fixtures/confusables.fixture.json`). NameNormalizer parity verified by `server/shared/typescript/geo/abstractions/tests/name-normalizer.test.ts` against the same byte-equivalent corpus.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Temporal primitives + three-category model — `IClock` injection seam + `SystemClock` (production) / `TestClock` (deterministic, thread-safe) + Category 1 `ZonedInstant` (past instant + IANA tz context) + Category 3 `LocalAnchoredEvent` (local wall-clock + IANA tz + denormalized UTC fire instant). Cross-language structural parity: same field names, same nullability semantics, byte-identical canonical IANA output on both sides. | `D2.Shared.Time` (`server/shared/dotnet/time/`) — `IClock.GetCurrentInstant()` returning `NodaTime.Instant`; sealed records with private positional constructor + static `Create(...) → D2Result<T>` smart-constructor factory (IANA validation via `DateTimeZoneProviders.Tzdb.GetZoneOrNull` + canonical normalization via `TimeZoneIdNormalizer` over `TzdbDateTimeZoneSource.Default.CanonicalIdMap`). `LocalAnchoredEvent.ComputeNextFire()` encapsulates `Resolvers.LenientResolver` (skipped local times map forward to post-gap; ambiguous local times pick the earlier instant). EF Core integration: `AddD2NodaTime()` extension on `NpgsqlDbContextOptionsBuilder` wires the Npgsql NodaTime value-converter suite. | `@d2/time` (`server/shared/typescript/time/`) — `IClock.getInstant()` returning `Temporal.Instant`; classes with private constructor + static `create(...) → D2Result<T>` smart-constructor factory (IANA validation via `Intl.DateTimeFormat` + canonical normalization via `resolvedOptions().timeZone` + `sr_ianaAliasOverrides` map bridging Node Intl ↔ NodaTime `CanonicalIdMap` deltas — `Asia/Saigon`, `Asia/Calcutta`, Argentina prefix preferences, etc.). `LocalAnchoredEvent.computeNextFire()` uses `Temporal.PlainDateTime.toZonedDateTime(zone, { disambiguation: "compatible" })` matching .NET's `LenientResolver`. Polyfilled via `temporal-polyfill@0.3.2` for runtimes without native `Temporal`. Optional `nextFireUtc?: Temporal.Instant` (no `null` token; absence = absent — per the TS `prefer-undefined-over-null` convention). | —               | Hand-written types on both sides (not spec-driven codegen — temporal-API maturity differs across languages). Cross-language parity verified by `contracts/temporal/temporal-adversarial.fixture.json` (8 fixture cases: US/EU/AU spring-forward, US/EU fall-back, IANA alias normalization `Asia/Saigon → Asia/Ho_Chi_Minh` + `US/Pacific → America/Los_Angeles`, unambiguous baseline) — both `.NET` `CrossLanguageTemporalParityTests` and TS `cross-language.test.ts` load the fixture and assert byte-identical `expectedUtc` output. Documented cross-language divergences (cannot be eliminated without papering over engine behavior): (a) `:60` seconds — Temporal clamps to `:59` (no throw), NodaTime throws on `second=60`; both engines reject leap seconds, only the error mode differs. (b) IANA case-sensitivity — Node Intl is case-INSENSITIVE (`"america/new_york"` → `"America/New_York"`), NodaTime tzdb lookup IS case-sensitive (returns null for non-canonical case). (c) Year range — Temporal supports `[-271821, +275760]`, NodaTime ISO calendar supports `[-9998, 9999]`; callers crossing the boundary MUST stay within the NodaTime range. Naming: same Category-1/3 shape, same IANA-identifier field name (`IANAIdentifier` PascalCase .NET / `ianaIdentifier` camelCase TS — language conventions). |

---

## Utility helper naming divergence — TS `*Undef*` vs .NET `*Null*`

The `@d2/utilities` TS package and `D2.Shared.Utilities.Extensions` .NET package provide
identical boundary-helper behavior but use different absent-value tokens and function-name
suffixes per language convention:

| TS (`@d2/utilities`)              | .NET (`D2.Shared.Utilities.Extensions`) | Return on absent               |
| --------------------------------- | --------------------------------------- | ------------------------------ |
| `toUndefIfEmpty(s)`               | `ToNullIfEmpty()` on string             | TS: `undefined` / .NET: `null` |
| `tryParseTruthyUndefUuid(s)`      | `TryParseTruthyNull(out Guid? r)`       | TS: `undefined` / .NET: `null` |
| `tryParseTruthyUndefInt(s)`       | `TryParseTruthyNull(out int? r)`        | TS: `undefined` / .NET: `null` |
| `tryParseTruthyUndefEnum(obj, s)` | `TryParseTruthyNull<TEnum>(out var r)`  | TS: `undefined` / .NET: `null` |

**Rationale**: TypeScript's native absent-value is `undefined` (rules.md §6.15 — prefer
`undefined` for absent semantics everywhere in TS, never `null`). C# convention uses `null`
for nullable reference/value types. The divergence is intentional and permanent — behavior
is functionally equivalent on the helpers' input domain (same input → same absent/present
semantic), only the absent sentinel and function-name suffix differ.

Cross-language reviewers reading both sides should expect `*Undef*` names in TS returning
`undefined`, and `*Null*` names in the corresponding .NET methods returning `null` — same
semantics, different sentinel per language convention. Per-language adversarial test
coverage lives in `server/shared/typescript/utilities/tests/{parse,strings,clean,falsey}.test.ts`
and `server/shared/dotnet/utilities/Tests/D2.Shared.Tests.Unit.Utilities.*`. No drift — this
is the designed API surface.

---

## Process

When adding a cross-language component:

1. **Identify the concept** — what abstraction is being shared?
2. **Identify the consumers** — which languages have the same problem?
3. **Justify exclusivity** for any language NOT in the implementation set (using the framework above)
4. **Add a row** to the parity table above (or create a new table for the component category)
5. **Document the API contract** in shared docs — both implementations must match
6. **Verify drift** during audit rounds (per [`docs/dev/rules.md §9`](dev/rules.md#9-architectural-layer-hygiene) cross-language drift predicate)

---

## Anti-Patterns

- **Implementing in language A, "porting later"** — if the other language needs it, port now or document why not
- **Diverging APIs across languages** — if D2Result has a `BubbleFail` method in .NET, it has the same method in TS (with adjusted naming convention)
- **Implementing twice with subtle differences** — one of them WILL drift. Either share via codegen or document divergences explicitly.
- **Not tracking parity at all** — over time, drift accumulates silently. The table is the source of truth.

---

## Parity test infrastructure

Cross-language parity for spec-driven catalogs is enforced by a fixture-driven test pair: the .NET test suite emits canonical JSON fixtures to disk; a TS-side Vitest workspace reads those fixtures and asserts that the spec-emitted TypeScript decoders / encoders / catalogs agree byte-for-byte (after canonicalization).

**Architectural shape**

```
.NET test                                    TS Vitest test
[Trait("Category","ContractFixtures")]       (in @d2/contract-tests)
       |                                              ^
       v                                              |
  Emit fixture JSON                              Read fixture JSON
  to disk                                        from disk
       |                                              |
       └────────────► same path ◄────────────────────┘
            server/shared/typescript/contract-tests/
            fixtures/<catalog>/<scenario>.json
```

The fixture file is the meeting point. Fixtures are committed to git so PR diffs surface accidental drift. There is no subprocess, no JSON-RPC bridge, no live spawn-and-roundtrip — comparison is deterministic across two independent test runs (one per language).

**Direction**: forward-only (`.NET → fixture → TS read+assert`). Bidirectional cross-language assertion (TS-emit → .NET-read) is intentionally OUT OF SCOPE for this test surface.

**Catalogs covered (initial set)**

- `propagated-context/` — `IPropagatedContext` envelope + per-spec-field cap boundaries
- `auth-context/` — `IAuthContext` typed-shape property surface
- `request-context/` — `IRequestContext` typed-shape property surface (transitive `IAuthContext` + own properties)
- `jwt-payload/` — `JwtPayload` typed-shape vs spec-emitted `JwtClaimTypes` constants
- `redact-paths/` — `IAuthContextRedactPaths` / `IRequestContextRedactPaths` arrays vs `[RedactData]`-attributed properties on the .NET interfaces
- `headers/` — `CommonHeaders` / `HttpHeaders` / `AmqpHeaders` / `GrpcHeaders` `as const` membership and wire values
- `error-codes/` — generic D2Result `ErrorCodes` constant map + `getErrorHttpStatus` per-code mapping (scenarios: `codes` and `http-statuses`)
- `problem-details/` — RFC 7807 ProblemDetails wire catalog (`TYPE_URI_PREFIX` + `CONTENT_TYPE` + extension keys + per-HTTP-status titles)
- `tk-message/` — TKMessage JSON wire shape (`KEY` / `PARAMS` property-name constants) + round-trip fixtures (`round-trip-no-params` / `round-trip-with-params` / `round-trip-with-multiple-params`) capturing the byte shape produced by `TKMessageJsonConverter`
- `input-error/` — InputError JSON wire shape (`FIELD` / `ERRORS` property-name constants) + round-trip fixtures (`round-trip-single-error` / `round-trip-multiple-errors` / `round-trip-dot-notation-field`) including nested TKMessage shapes
- `d2result-envelope/` — D2Result Shape B wire envelope (7 `D2ResultEnvelopeFieldNames` property-name constants) + round-trip fixtures (`round-trip-ok` / `round-trip-ok-with-data` / `round-trip-not-found` / `round-trip-validation-failed` / `round-trip-with-trace-id`) capturing the byte shape produced by `JsonSerializer.Serialize(D2Result)` — pins canonical camelCase wire keys + the absence-of-`Is*`-helper-leakage invariant
- `grpc-trailers/` — `D2GrpcTrailers` catalog membership + wire values (gRPC trailer Metadata-key constants for cross-service error propagation). Cross-language drift on trailer wire keys (e.g. the canonical camelCase `traceId` casing) is structurally impossible.
- `otel-messaging-tags/` — `MessagingActivityTags` catalog membership + wire values (OTel activity-tag attribute-name constants for messaging publisher + consumer Activity.SetTag calls). Closes the `messaging.operation` vs `messaging.operation.type` semconv drift bug.
- `encryption-domains/` — `EncryptionDomains` closed-enum catalog membership + wire values (keyring-domain identifiers). Cross-language critical — TS-side `@d2/encryption-abstractions` consumes the same closed-enum identifiers when decoding the on-wire encryption frame.
- `dlq-failure-metadata/` — `DlqFailureMetadataFields` (6 JSON property-name constants) + `DlqFailureCauses` (5 closed-enum cause-string constants) catalogs. Consumed by the TS-side `@d2/messaging-abstractions` package.
- `encryption-frame/` — `EncryptionFrame` binary-layout field-offset + byte-length constants + the `CONSTRAINT_*` cap constants. Source of truth for the TS-side `@d2/encryption-abstractions` encryption-frame catalog; cross-language byte-for-byte parity on the binary frame is structurally guaranteed — both sides read the same spec.
- `contracts/geo/fixtures/confusables.fixture.json` (NOT under `contract-tests/fixtures/` — lives next to the geo specs) — adversarial fixture for the geo name resolver. Each row pins `{ input, expectedAlpha2 }` (countries) or `{ input, parentCountryAlpha2, expectedIso31662 }` (subdivisions); `null` expected means "must NOT silently guess". Both runtimes (.NET `D2.Shared.Tests.Unit.Geo.Default.NameResolver.ConfusablesTests` + TS `@d2/geo-default/tests/name-resolution/confusables.test.ts`) load the fixture directly and run the resolver against every row. Append-only — new rows added on regression discovery; rows are never removed.

**Invocation**

```bash
# 1. Regenerate fixtures from the .NET side:
dotnet test server/D2.slnx --filter "Category=ContractFixtures"

# 2. Run the TS parity assertions:
pnpm test:contracts        # from repo root
```

**CI hook**

`.github/workflows/test.yml` carries commented-out CI job blocks for `contract-fixtures-emit` (regenerates fixtures + asserts no `git diff` drift) and `contract-tests-parity` (runs the Vitest assertions). They activate alongside the .NET `build` job.

**Adding a catalog**

1. Pick a synthetic-data scenario set (every fixture uses synthetic IDs / RFC 5737 IPs / `*.invalid` emails — no real PII).
2. Add a `<catalog>FixtureEmitter.cs` under `server/shared/dotnet/tests/Integration/ContractFixtures/` with one `[Fact, Trait("Category","ContractFixtures")]` per scenario; write via `FixturePathHelpers.WriteFixture`.
3. Add a `<catalog>.parity.test.ts` under `server/shared/typescript/contract-tests/tests/` that loads each scenario via `loadFixture` and asserts per-VALUE / per-PROPERTY equality.
4. Update the parent `server/shared/typescript/README.md` Mermaid graph + this doc's catalog list.
