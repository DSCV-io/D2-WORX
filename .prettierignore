# -----------------------------------------------------------------------
# Copyright (c) DCSV. All rights reserved.
# -----------------------------------------------------------------------

# Build output.
**/dist/**
**/node_modules/**
**/.svelte-kit/**
**/coverage/**

# Codegen-emitted source — formatting is the emitter's responsibility.
**/*.g.ts
**/*.g.json
public/packages/typescript/protos/src/generated/**
public/packages/typescript/protos/gen/**
public/packages/typescript/typespec-emitters/tests/grpc-fixtures/generated/**
private/services/web/src/lib/paraglide/**
# Legacy pre-reorg paths (kept until fully scrubbed from tooling).
server/shared/typescript/protos/src/generated/**
server/shared/typescript/typespec-emitters/tests/grpc-fixtures/generated/**
server/web/src/lib/paraglide/**

# Machine-emitted cross-language parity fixtures.
# Written by the .NET ContractFixtures test suite via System.Text.Json with
# WriteIndented=true; the parity test reads the JSON regardless of layout, so
# pretty-print equivalence with Prettier's print-width / array-collapse rules
# is intentionally not enforced. Format is the .NET emitter's responsibility.
public/packages/typescript/contract-tests/fixtures/**
server/shared/typescript/contract-tests/fixtures/**

# Frozen v1 snapshot.
old/**

# Claude harness scratch.
.claude/**

# Lockfiles.
pnpm-lock.yaml

# Markdown is NOT Prettier-formatted. Prettier's markdown emphasis-normalization
# (* <-> _) cannot distinguish an emphasis marker from an underscore inside an
# identifier or an asterisk in a glob, so it corrupts this identifier-heavy docs
# tree (e.g. SCREAMING_SNAKE -> SCREAMING*SNAKE, *.json -> _.json,
# PHASE_3.md -> PHASE*3.md). There is no Prettier option to disable that, so
# Markdown is governed by the doc conventions in rules.md §11, not Prettier.
**/*.md
