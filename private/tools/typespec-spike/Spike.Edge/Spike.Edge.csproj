<Project Sdk="Microsoft.NET.Sdk.Web">

  <!-- Spike A Edge host: the GENERATED EdgeReceiver.cs + GeneratedWiring.cs +
       a one-liner-ish chassis Program.cs. The host wires the whole multi-hop
       pipeline via AddGeneratedSpike()/MapGeneratedSpike() — no hand-written
       controllers/handlers. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Grpc.AspNetCore" Version="2.71.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../Spike.Contracts/Spike.Contracts.csproj" />
    <ProjectReference Include="../Spike.Sse/Spike.Sse.csproj" />
  </ItemGroup>

</Project>
