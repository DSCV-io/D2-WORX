// <auto-generated> — emitted by @d2/spike-emitter from push.tsp. DO NOT EDIT.
syntax = "proto3";

package d2.spike.push.v1;

option csharp_namespace = "D2.Spike.Push.V1";

// gRPC service for the @d2ServerPush op "pushNotificationCreated" (owner: Edge).
service Push {
  // Inbound leg: microservice --(gRPC)--> Edge. Edge's generated receiver
  // forwards the payload to the SSE emit binding (push target: "user").
  rpc PushNotificationCreated(NotificationCreated) returns (PushAck);
}

// Request payload — travels every hop unchanged (A3 payload-identity claim).
message NotificationCreated {
  string id = 1;
  string message = 2;
}

// Unary ack returned once the payload reached the SSE emit binding.
message PushAck {
  bool accepted = 1;
}
