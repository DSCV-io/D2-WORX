{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "D² audience spec",
  "description": "Schema for contracts/auth-audiences/audiences.spec.json — the source-of-truth catalog that DcsvIo.D2.Auth.Audiences.SourceGen reads to emit the Audiences static class into DcsvIo.D2.Auth.Abstractions. Each entry surfaces as a 'public const string {Name} = \"{url}\"' on the emitted Audiences class plus name→url / url→name lookup helpers.",
  "type": "object",
  "required": ["audiences"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "audiences": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/audience" }
    }
  },
  "definitions": {
    "audience": {
      "type": "object",
      "required": ["name", "url"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[A-Z][A-Za-z0-9]*$",
          "description": "PascalCase identifier — emitted as the const name (e.g. 'Files' → Audiences.Files). Must start with an uppercase letter and contain only letters and digits."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "Absolute URI used as the JWT 'aud' claim value AND as the targetAudience argument to TokenExchangeClient.ExchangeAsync. The const's value is this URL string."
        },
        "description": {
          "type": "string",
          "description": "Free-form description rendered as XML doc on the emitted constant."
        },
        "deprecated": {
          "type": "boolean",
          "description": "When true, the entry is deprecated: the generated C# constant + factory carry [Obsolete] and the TS constant + factory carry @deprecated. Additive — the entry is NEVER deleted. Deleting a deprecated (or non-deprecated) entry is a breaking change the JSON-diff gate fails."
        },
        "deprecatedReason": {
          "type": "string",
          "minLength": 1,
          "description": "Plain dev-facing English explaining why the code is deprecated; rendered into the [Obsolete(\"...\")] message, the @deprecated JSDoc, and the changelog entry. NOT a TK key (developer-facing, not user-facing)."
        },
        "replacedBy": {
          "type": "string",
          "description": "Wire code of the successor entry (e.g. RESOURCE_NOT_FOUND). Must reference a real, non-deprecated entry in the merged registry. Omit for pure retirement with no replacement."
        },
        "sunset": {
          "type": "string",
          "format": "date",
          "description": "ISO-8601 date for the future RFC 8594 Sunset response header. Edge-runtime-deferred: carried on the contract now, consumed by the Edge response middleware when it exists. Inert until then."
        }
      }
    }
  }
}
