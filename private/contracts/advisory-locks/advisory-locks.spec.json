{
  "$schema": "./schema.json",
  "locks": [
    {
      "constName": "MIGRATOR",
      "database": "d2-keycustodian",
      "key": 1001001001,
      "doc": "Blocking startup-migration lock for d2-keycustodian. Held by AdvisoryLockMigrator during ensure-db + MigrateAsync. Separate key from ROTATION so the migrator and the rotation timer never block each other."
    },
    {
      "constName": "ROTATION",
      "database": "d2-keycustodian",
      "key": 2002002002,
      "doc": "Try-lock guarding unattended rotation ticks for d2-keycustodian. Held by KeyRotationService for the duration of one RunDueRotations invocation. Skip-if-held semantics prevent two instances from running simultaneous rotations."
    },
    {
      "constName": "CA_SEED",
      "database": "d2-keycustodian",
      "key": 4004004004,
      "doc": "Try-lock guarding startup certificate-authority seeding for d2-keycustodian. Held by CaSeedingService while it seeds the root + intermediate from the CA provider. Separate key from MIGRATOR and ROTATION; skip-if-held semantics ensure exactly one instance seeds across a multi-instance deployment."
    }
  ]
}
