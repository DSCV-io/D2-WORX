<!--
Copyright (c) DCSV. All rights reserved.
-->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>DcsvIo.D2.Private.Edge.KeyCustodian.App</RootNamespace>
  </PropertyGroup>

  <!--
    Application layer (EF-as-DDD) — two sections. Application/ holds the per-op
    command handlers (BaseRepoHandler: load the tracked KeyRecord, invoke the
    domain transition on the rehydrated aggregate, ProjectOnto the Record, one
    SaveChangesAsync — an ordinary UPDATE plus the audit INSERT) + the query
    handlers (BaseHandler, AsNoTracking) + Observability + the DI extension.
    Infrastructure/ holds the App-owned ports + shapes (the IKeyCustodianDbContext
    seam + flat records + mapper, the announcer + root-key-provider ports, and the
    options + rotation-policy-provider). References EF Core directly (EF-as-DDD:
    the DbContext interface lives here); Domain stays EF-free.
  -->

  <ItemGroup>
    <!--
      DcsvIo.D2.Private.Edge.KeyCustodian.Domain — the aggregates + state-machine transitions
      that App orchestrates via command/query handlers.
    -->
    <ProjectReference Include="..\domain\DcsvIo.D2.Private.Edge.KeyCustodian.Domain.csproj" />
    <!--
      DcsvIo.D2.Handler — BaseHandler (query handlers' cross-cutting: telemetry /
      metrics / D2Result / cancellation / scope).
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)handler\core\DcsvIo.D2.Handler.csproj" />
    <!--
      DcsvIo.D2.Handler.Repo — BaseRepoHandler (command handlers; DB-exception →
      D2Result via injected IDbExceptionClassifier). Also brings
      Microsoft.EntityFrameworkCore + DcsvIo.D2.Handler + DcsvIo.D2.Result
      transitively, but each is listed explicitly ("declare what you use").
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)handler\repo\DcsvIo.D2.Handler.Repo.csproj" />
    <!--
      DcsvIo.D2.Result — D2Result<T> semantic factories used directly by command /
      query handlers even though Handler.Repo carries it transitively.
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)result\core\DcsvIo.D2.Result.csproj" />
    <!--
      DcsvIo.D2.Encryption — IPayloadCrypto (root-wrap of KeyMaterialEncrypted
      happens in command handlers); PayloadCryptoKeyring resolution.
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)encryption\core\DcsvIo.D2.Encryption.csproj" />
    <!--
      DcsvIo.D2.Time — IClock for RotationPlanner due-key math + transition
      timestamps inside command handlers.
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)time\DcsvIo.D2.Time.csproj" />
    <!--
      DcsvIo.D2.Utilities — [RedactData] on CompromiseKeyInput.Reason + Falsey()
      input guards. Brought transitively via Domain but listed explicitly
      ("declare what you use").
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)utilities\DcsvIo.D2.Utilities.csproj" />
    <!--
      DcsvIo.D2.I18n.Keys — the generated TK.* translation-key constants the App
      passes to D2Result factories (e.g. the precondition arg-naming messages).
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)i18n\keys\DcsvIo.D2.I18n.Keys.csproj" />
    <!--
      DcsvIo.D2.Private.Edge.KeyCustodian.Client — the consumer-facing client package that holds
      transport DTOs for exposed operations, the module façade interface, and the
      keyring consumer runtime. App references Client so I<Op>Handler + handler
      bodies can use the client DTOs directly, and so the module's in-process
      keyring source can compose the client-package hot-swap machinery
      (App ← Client is the legal edge; Client refs contracts + shared libs only).
    -->
    <ProjectReference Include="..\client\DcsvIo.D2.Private.Edge.KeyCustodian.Client.csproj" />
    <!--
      DcsvIo.D2.Context.Abstractions — the scoped IRequestContext the sign handler
      reads (the established Origin / ImmediateCaller) and the JWT-minter capability
      injects to assert its in-process plane. Read side only — App stays gRPC-free +
      ASP.NET-Core-free. Brought transitively via DcsvIo.D2.Handler but listed
      explicitly ("declare what you use").
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)context\abstractions\DcsvIo.D2.Context.Abstractions.csproj" />
    <!--
      DcsvIo.D2.Auth.Abstractions — IJwksProvider + JwksKeySetSnapshot for the
      issuer-host in-process JWKS provider (replaces HttpJwksProvider on Edge only).
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)auth\abstractions\DcsvIo.D2.Auth.Abstractions.csproj" />
    <!--
      DcsvIo.D2.Resilience — Singleflight dedupes concurrent in-process JWKS refreshes
      (rotation backplane + reactive unknown-kid).
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)resilience\DcsvIo.D2.Resilience.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Microsoft.EntityFrameworkCore — EF-as-DDD: IKeyCustodianDbContext exposes
      DbSet<…> + SaveChangesAsync as public API. App owns the DbContext interface;
      Infra provides the concrete implementation. Listed explicitly because App
      genuinely uses EF types ("declare what you use" — mirrors DataGovernance.EFC
      and DcsvIo.D2.Handler.Repo conventions).
    -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <!--
      Microsoft.Extensions.Options — IOptions<KeyCustodianOptions> consumed by the
      generate / compromise command handlers (key sizing) + the rotation-policy provider.
    -->
    <PackageReference Include="Microsoft.Extensions.Options" />
    <!--
      Microsoft.Extensions.DependencyInjection.Abstractions — the AddD2KeyCustodianApp
      DI extension + the [FromKeyedServices] root-crypto injection attribute.
      Brought transitively via DcsvIo.D2.Handler but listed explicitly.
    -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="DcsvIo.D2.Private.Edge.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="$(D2PrivatePackagesDotnetRoot)auth\abstractions-extensions\DcsvIo.D2.Private.Auth.Abstractions.Extensions.csproj" />
    <ProjectReference Include="$(D2PrivatePackagesDotnetRoot)encryption\extensions\DcsvIo.D2.Private.Encryption.Extensions.csproj" />
  </ItemGroup>
</Project>