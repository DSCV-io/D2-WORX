<!--
Copyright (c) DCSV. All rights reserved.
-->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>D2.Edge.KeyCustodian.App</RootNamespace>
  </PropertyGroup>

  <!--
    Application layer (EF-as-DDD) — two sections. Application/ holds the per-op
    command handlers (BaseRepoHandler: load the tracked KeyRecord, invoke the
    domain transition on the rehydrated aggregate, ProjectOnto the Record, one
    SaveChangesAsync — an ordinary UPDATE plus the audit INSERT) + the query
    handlers (BaseHandler, AsNoTracking) + Observability + the DI extension.
    Infrastructure/ holds the App-owned ports + shapes (the IKeyCustodianDbContext
    seam + flat records + mapper, the announcer + root-key-provider ports, and the
    options + rotation-policy-provider). References EF Core directly (EF-as-DDD:
    the DbContext interface lives here); Domain stays EF-free.
  -->

  <ItemGroup>
    <!--
      D2.Edge.KeyCustodian.Domain — the aggregates + state-machine transitions
      that App orchestrates via command/query handlers.
    -->
    <ProjectReference Include="..\domain\D2.Edge.KeyCustodian.Domain.csproj" />
    <!--
      D2.Shared.Handler — BaseHandler (query handlers' cross-cutting: telemetry /
      metrics / D2Result / cancellation / scope).
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)handler\core\D2.Shared.Handler.csproj" />
    <!--
      D2.Shared.Handler.Repo — BaseRepoHandler (command handlers; DB-exception →
      D2Result via injected IDbExceptionClassifier). Also brings
      Microsoft.EntityFrameworkCore + D2.Shared.Handler + D2.Shared.Result
      transitively, but each is listed explicitly ("declare what you use").
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)handler\repo\D2.Shared.Handler.Repo.csproj" />
    <!--
      D2.Shared.Result — D2Result<T> semantic factories used directly by command /
      query handlers even though Handler.Repo carries it transitively.
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)result\core\D2.Shared.Result.csproj" />
    <!--
      D2.Shared.Encryption — IPayloadCrypto (root-wrap of KeyMaterialEncrypted
      happens in command handlers); PayloadCryptoKeyring resolution.
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)encryption\core\D2.Shared.Encryption.csproj" />
    <!--
      D2.Shared.Time — IClock for RotationPlanner due-key math + transition
      timestamps inside command handlers.
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)time\D2.Shared.Time.csproj" />
    <!--
      D2.Shared.Utilities — [RedactData] on CompromiseKeyInput.Reason + Falsey()
      input guards. Brought transitively via Domain but listed explicitly
      ("declare what you use").
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)utilities\D2.Shared.Utilities.csproj" />
    <!--
      D2.Shared.I18n.Keys — the generated TK.* translation-key constants the App
      passes to D2Result factories (e.g. the precondition arg-naming messages).
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)i18n\keys\D2.Shared.I18n.Keys.csproj" />
    <!--
      D2.Edge.KeyCustodian.Client — the consumer-facing client package that holds
      transport DTOs for exposed operations, the module façade interface, and the
      keyring consumer runtime. App references Client so I<Op>Handler + handler
      bodies can use the client DTOs directly, and so the module's in-process
      keyring source can compose the client-package hot-swap machinery
      (App ← Client is the legal edge; Client refs contracts + shared libs only).
    -->
    <ProjectReference Include="..\client\D2.Edge.KeyCustodian.Client.csproj" />
    <!--
      D2.Shared.Context.Abstractions — the scoped IRequestContext the sign handler
      reads (the established Origin / ImmediateCaller) and the JWT-minter capability
      injects to assert its in-process plane. Read side only — App stays gRPC-free +
      ASP.NET-Core-free. Brought transitively via D2.Shared.Handler but listed
      explicitly ("declare what you use").
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)context\abstractions\D2.Shared.Context.Abstractions.csproj" />
    <!--
      D2.Shared.Auth.Abstractions — IJwksProvider + JwksKeySetSnapshot for the
      issuer-host in-process JWKS provider (replaces HttpJwksProvider on Edge only).
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)auth\abstractions\D2.Shared.Auth.Abstractions.csproj" />
    <!--
      D2.Shared.Resilience — Singleflight dedupes concurrent in-process JWKS refreshes
      (rotation backplane + reactive unknown-kid).
    -->
    <ProjectReference Include="$(D2PublicPackagesDotnetRoot)resilience\D2.Shared.Resilience.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Microsoft.EntityFrameworkCore — EF-as-DDD: IKeyCustodianDbContext exposes
      DbSet<…> + SaveChangesAsync as public API. App owns the DbContext interface;
      Infra provides the concrete implementation. Listed explicitly because App
      genuinely uses EF types ("declare what you use" — mirrors DataGovernance.EFC
      and D2.Shared.Handler.Repo conventions).
    -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <!--
      Microsoft.Extensions.Options — IOptions<KeyCustodianOptions> consumed by the
      generate / compromise command handlers (key sizing) + the rotation-policy provider.
    -->
    <PackageReference Include="Microsoft.Extensions.Options" />
    <!--
      Microsoft.Extensions.DependencyInjection.Abstractions — the AddD2KeyCustodianApp
      DI extension + the [FromKeyedServices] root-crypto injection attribute.
      Brought transitively via D2.Shared.Handler but listed explicitly.
    -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="D2.Edge.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="$(D2PrivatePackagesDotnetRoot)auth\abstractions-extensions\D2.Shared.Auth.Abstractions.Extensions.csproj" />
    <ProjectReference Include="$(D2PrivatePackagesDotnetRoot)encryption\extensions\D2.Shared.Encryption.Extensions.csproj" />
  </ItemGroup>
</Project>