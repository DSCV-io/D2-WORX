{
  "emittedBy": "private/services/edge/key-custodian/client-ts/scripts/emit-csr-fixtures.fixture.ts",
  "regenerate": "pnpm --filter @d2/key-custodian-client emit-csr-fixtures",
  "note": "ECDSA signatures are randomized: regeneration produces new bytes with the same verdicts. The .NET NodeLeafClient tests drive the REAL CsrVerification + issuance rules over these files.",
  "fixtures": [
    {
      "file": "valid-p256.csr.fixture.der",
      "expectedVerdict": "issues",
      "description": "The production CSR construction: fresh P-256 key, CN=d2-workload subject, ECDSA-SHA256 proof-of-possession. Verifies and issues; the leaf SAN is the harness-supplied authenticated identity."
    },
    {
      "file": "foreign-cn.csr.fixture.der",
      "expectedVerdict": "issues-subject-ignored",
      "description": "Well-formed P-256 CSR whose subject claims another service (CN=files). KeyCustodian structurally ignores the subject: it verifies, issues, and the leaf SAN is STILL the authenticated identity — impersonation via CSR subject is unrepresentable."
    },
    {
      "file": "wrong-curve-p384.csr.fixture.der",
      "expectedVerdict": "rejected-invalid-csr",
      "description": "Well-formed CSR over a P-384 key. The leaf key policy accepts ONLY the P-256 named curve (by curve OID), so verification rejects it."
    },
    {
      "file": "oversized.csr.fixture.der",
      "expectedVerdict": "rejected-invalid-csr",
      "description": "Real P-256 CSR inflated past the 4096-byte DER cap by a large requested SAN extension. The size bound rejects it BEFORE any ASN.1 parse."
    },
    {
      "file": "tampered-pop.csr.fixture.der",
      "expectedVerdict": "rejected-invalid-csr",
      "description": "Valid CSR with one flipped byte in the trailing signature — the proof-of-possession self-signature fails verification."
    }
  ]
}
