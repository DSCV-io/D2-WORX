// -----------------------------------------------------------------------
// <auto-generated>
//   Generated by the @d2/typespec-emitters TypeSpec emitter.
//   Source spec: contracts/typespec/fixtures/sign-shaped.tsp
//   Manual edits will be lost on rebuild.
// </auto-generated>
// -----------------------------------------------------------------------

/* eslint-disable */
// @ts-nocheck
// Generated browser REST client. Each method delegates to the real $lib browser substrate
// (apiCall / apiCallAnon → fetch + auth + ProblemDetails-to-D2Result). The $lib import + the
// DTO imports resolve only inside the SvelteKit BFF — the emitted file is plain runtime
// JS; the byte-gate pins the bytes and the behavioral test drives it against a faithful double.

import { apiCall } from "$lib/client/rest/gateway-client.js";
import type { D2Result } from "@d2/result";

// Emitted DTO types — paths resolve in the SvelteKit BFF consumer.
import type { SignFixtureInput, SignFixtureOutput } from "./sign-fixture-dto.js";

/** Per-call options for a generated REST client method. */
export interface RestCallOptions {
  /** Cooperative-cancellation signal forwarded to fetch. */
  readonly signal?: AbortSignal;
  /** Per-call timeout (ms) forwarded to the substrate. */
  readonly timeout?: number;
}

/** Per-call options for an idempotent (header keySource) REST client method. */
export interface RestCallOptionsWithIdempotency extends RestCallOptions {
  /** Idempotency-Key header value (caller-supplied for the header keySource). */
  readonly idempotencyKey?: string;
}

/** Generated browser REST client interface for the SignFixture module. */
export interface SignFixtureRestClient {
  signFixture(input: SignFixtureInput, opts?: RestCallOptionsWithIdempotency): Promise<D2Result<SignFixtureOutput>>;
  signFixtureDerived(input: SignFixtureInput, opts?: RestCallOptions): Promise<D2Result<SignFixtureOutput>>;
}

/** Generated browser REST client for the SignFixture module. */
export const signFixtureRestClient: SignFixtureRestClient = {
  signFixture(input, opts) {
    return apiCall<SignFixtureOutput>("/internal/v1/fixtures/sign-fixture", {
      method: "POST",
      body: input,
      idempotencyKey: opts?.idempotencyKey,
      signal: opts?.signal,
      timeout: opts?.timeout,
    });
  },
  signFixtureDerived(input, opts) {
    return apiCall<SignFixtureOutput>("/internal/v1/fixtures/sign-fixture-derived", {
      method: "POST",
      body: input,
      signal: opts?.signal,
      timeout: opts?.timeout,
    });
  }
};
