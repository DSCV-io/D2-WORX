// -----------------------------------------------------------------------
// <auto-generated>
//   Generated by tools/ts-codegen — see this script's source for details.
//   Source spec: contracts/input-error/input-error.spec.json
//   Manual edits will be lost on rebuild.
// </auto-generated>
// -----------------------------------------------------------------------

/* eslint-disable */

/**
 * Spec-derived JSON property-name catalog for the InputError wire shape. Every JSON serializer /
 * deserializer in TypeScript references these constants for property names instead of inline string
 * literals — drift between the wire and the code is structurally impossible.
 *
 * Cross-language parity: the SAME spec drives the .NET-side catalog via
 * D2.Shared.WireShapes.SourceGen. Both sides emit the same property names byte-for-byte;
 * cross-language wire drift is impossible.
 */
export const InputErrorWireShape = {
  /**
   * JSON property name carrying the offending input field name (e.g. 'email', 'address.city').
   * REQUIRED on every InputError; dot-notation paths supported for nested fields.
   */
  FIELD: "field",
  /**
   * JSON property name carrying the array of TKMessage objects describing what's wrong with the
   * field. REQUIRED; one-or-more TKMessage entries per InputError — each TKMessage uses the
   * spec-derived 'key'/'params' shape from contracts/tk-message.
   */
  ERRORS: "errors",
} as const;
