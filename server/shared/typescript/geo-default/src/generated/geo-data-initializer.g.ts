// -----------------------------------------------------------------------
// <auto-generated>
//   Generated by tools/ts-codegen — see this script's source for details.
//   Source spec: contracts/geo/*.spec.json
//   Manual edits will be lost on rebuild.
// </auto-generated>
// -----------------------------------------------------------------------

/* eslint-disable */

import { wireCountryNav } from "./countries.g.js";
import { wireCurrencyNav } from "./currencies.g.js";
import { wireGeopoliticalEntityNav } from "./geopolitical-entities.g.js";
import { wireLanguageNav } from "./languages.g.js";
import { wireLocaleNav } from "./locales.g.js";
import { wireSubdivisionNav } from "./subdivisions.g.js";
import { wireTimezoneNav } from "./timezones.g.js";

let _initialized = false;

/**
 * Coordinator for the two-pass populate pattern.
 * Idempotent — short-circuits on subsequent calls via the
 * `_initialized` flag. Each catalog's first pass runs as a top-level
 * block during this module's import-graph evaluation; the wire-nav
 * step (this function) wires the cross-catalog nav refs.
 */
export function initializeGeoData(): void {
  if (_initialized) return;
  _initialized = true;

  // Wire-nav step — wire nav refs in dependency order.
  // SubdivisionLookup.byCountry must be populated before
  // CountryLookup.wireCountryNav consumes it for Country.subdivisions.
  wireSubdivisionNav();
  wireCountryNav();
  wireLocaleNav();
  wireCurrencyNav();
  wireLanguageNav();
  wireTimezoneNav();
  wireGeopoliticalEntityNav();
}

// Top-level call — importing this module guarantees the catalogs are
// fully wired before consumer code reads them. ESM module caching
// makes repeat imports a no-op.
initializeGeoData();