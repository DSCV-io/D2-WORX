<!--
Copyright (c) DCSV. All rights reserved.
-->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>D2.Shared.DataGovernance.Abstractions</RootNamespace>
  </PropertyGroup>

  <!--
    PURE marker / attribute / seam library. Zero EF, zero DI, zero Utilities.
    Domain and host code reference the engine seam (IAnonymizationEngine) and
    the ownership / trackable markers (IUserOwned, IOrgOwned,
    IExemptFromAnonymization, IAnonymizationTrackable) without pulling EF Core.
    Implementations — attribute-mapping convention, fluent API, anonymization
    engine, startup guard, and DI registration — live in
    D2.Shared.DataGovernance.EntityFrameworkCore.
  -->

  <ItemGroup>
    <!--
      D2.Shared.Result — IAnonymizationEngine returns
      Task<D2Result<AnonymizationOutcome>>. This is the only dependency;
      the purity mandate prohibits references to EF Core, DI, Utilities,
      Geo, or any other infrastructure lib.
    -->
    <ProjectReference Include="..\..\result\core\D2.Shared.Result.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      JetBrains.Annotations — [UsedImplicitly] on AnonymizableAttribute's
      reflectively-consumed properties (Kind, ConstantValue, Template).
      The EF Core model convention reads them via reflection; without
      [UsedImplicitly] JetBrains inspectcode flags them as "never accessed".
      JetBrains.Annotations does not flow transitively from D2.Shared.Result
      (result/core has no JetBrains dep), so an explicit reference is required.
      Annotations-only package — acceptable for a pure abstractions lib;
      mirrors the pattern in utilities/D2.Shared.Utilities.csproj.
    -->
    <PackageReference Include="JetBrains.Annotations" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="D2.Shared.Tests" />
  </ItemGroup>
</Project>
