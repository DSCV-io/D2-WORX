// Test fixture — alpha proto before (field 'b' present).
// Package: d2.fixture.v2alpha (pre-stable — gate-exempt).
// The isProtoGateExempt exemption means this pair should NOT trigger the gate.
syntax = "proto3";

package d2.fixture.v2alpha;

option csharp_namespace = "D2.Services.Protos.Fixture.V2Alpha";

message M {
  string a = 1;
  string b = 2;
}
