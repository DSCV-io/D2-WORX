name: PR

# Single-lane CI per V2.md §8.
#
# Active jobs ramp up as services come online per V2.md §4 phases.
# Jobs are commented out (TODO) until their target service exists.
# Total wall time target: <5 minutes (all active jobs run in parallel).
#
# Required gates (non-skippable once active):
#   - integration-key-rotation (per V2.md §5.4 KeyCustodian)

on:
  pull_request:
    branches: [main, nova]
  push:
    branches: [main, nova]

jobs:
  # ==========================================================================
  # Active now (no service dependencies)
  # ==========================================================================

  # TODO: proto-checks — enable when buf.yaml is committed
  # proto-checks:
  #   name: Proto checks (buf lint + format)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: bufbuild/buf-setup-action@v1
  #     - run: buf lint
  #     - run: buf format --diff --exit-code

  # TODO: lint-and-format — enable when SvelteKit deps are restored (Phase 7)
  # AND server/D2.slnx contains projects (Phase 0+)
  # lint-and-format:
  #   name: Lint + format
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "24"
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: 10
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: "10.0.x"
  #     - name: pnpm install (server/web)
  #       run: pnpm install --frozen-lockfile
  #       working-directory: server/web
  #     - name: ESLint + Prettier (server/web)
  #       run: |
  #         pnpm exec eslint .
  #         pnpm exec prettier --check .
  #       working-directory: server/web
  #     - name: dotnet build (StyleCop + analyzers)
  #       run: dotnet build server/D2.slnx --configuration Release
  #     - name: jb inspectcode
  #       run: |
  #         dotnet tool restore
  #         dotnet jb inspectcode server/D2.slnx --severity=WARNING --no-build

  # ==========================================================================
  # Active per phase (TODO — uncomment as each service ships per V2.md §4)
  # ==========================================================================

  # TODO: build — Phase 0 (shared libs ship first .NET projects into D2.slnx)
  # build:
  #   name: Build
  #   runs-on: ubuntu-latest
  #   needs: [lint-and-format]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: "10.0.x"
  #     - run: dotnet build server/D2.slnx --configuration Release

  # TODO: unit-tests-dotnet — Phase 0 (D2.Shared.Tests ships)
  # unit-tests-dotnet:
  #   name: Unit tests (.NET)
  #   runs-on: ubuntu-latest
  #   needs: [build]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: "10.0.x"
  #     - run: dotnet test server/D2.slnx --filter Category=Unit --no-build --configuration Release

  # TODO: unit-tests-web — Phase 7 (SvelteKit rebuilt with v2 deps)
  # unit-tests-web:
  #   name: Unit tests (web — Vitest browser mode)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "24"
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: 10
  #     - run: pnpm install --frozen-lockfile
  #       working-directory: server/web
  #     - run: pnpm exec vitest run
  #       working-directory: server/web

  # TODO: integration-edge — Phase 3 (Edge ships)
  # TODO: integration-files — Phase 6
  # TODO: integration-courier — Phase 5
  # TODO: integration-notifications — Phase 5
  # TODO: integration-audit — Phase 4
  #
  # Pattern (one per service):
  # integration-{service}:
  #   name: Integration tests ({service})
  #   runs-on: ubuntu-latest
  #   needs: [build]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: "10.0.x"
  #     - run: dotnet test server/services/{service}/tests --no-build --configuration Release

  # TODO: integration-key-rotation — REQUIRED GATE per V2.md §5.4
  # Phase 3 (KeyCustodian module ships with Edge)
  # integration-key-rotation:
  #   name: Key rotation (REQUIRED)
  #   runs-on: ubuntu-latest
  #   needs: [build]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: "10.0.x"
  #     - run: dotnet test server/services/edge/tests --filter Category=KeyRotation --no-build --configuration Release

  # TODO: web-component-tests — Phase 7
  # web-component-tests:
  #   name: Web component tests (Vitest browser mode)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "24"
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: 10
  #     - run: pnpm install --frozen-lockfile
  #       working-directory: server/web
  #     - run: pnpm exec vitest run --browser
  #       working-directory: server/web

  # TODO: web-mock-playwright-tests — Phase 7
  # web-mock-playwright-tests:
  #   name: Web Playwright (mocked)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "24"
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: 10
  #     - run: pnpm install --frozen-lockfile
  #       working-directory: server/web
  #     - run: pnpm exec playwright install chromium
  #       working-directory: server/web
  #     - run: pnpm exec playwright test
  #       working-directory: server/web
  #       env:
  #         D2_MOCK_INFRA: "true"

  # ==========================================================================
  # Placeholder — keeps the workflow valid while all real jobs are TODO.
  # Remove this once the first real job (proto-checks or lint-and-format) is enabled.
  # ==========================================================================
  noop:
    name: noop (placeholder until first real job enables)
    runs-on: ubuntu-latest
    steps:
      - name: noop
        run: echo "v2 wipe complete; CI jobs activate per V2.md §4 phases"
