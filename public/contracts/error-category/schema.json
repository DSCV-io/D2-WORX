{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "D2 error category spec",
  "description": "Schema for contracts/error-category/error-category.spec.json — the source-of-truth catalog of the closed ErrorCategory classification. Each category corresponds to one snake_case wire string carried on the result envelope (gRPC + JSON) so consumers can do generic class-based handling (e.g. retry any infrastructure_unavailable) without importing the producer's error-code catalog. The PascalCase enum member is derived from the wire string (validation_failure -> ValidationFailure). This catalog is the single source for the nine category wire strings — the same closed set the canonical error-codes schema enumerates for each error code's 'category' field. D2.Shared.ErrorCodes.Category.SourceGen reads it to emit the ErrorCategory enum + ErrorCategoryWire mapping + ErrorCategoryJsonConverter into D2.Shared.ErrorCodes.Category; tools/ts-codegen/src/error-category-emit.ts reads it to emit error-category.g.ts into @d2/error-category.",
  "type": "object",
  "required": ["categories"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "categories": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/category" }
    }
  },
  "definitions": {
    "category": {
      "type": "object",
      "required": ["wire", "doc"],
      "additionalProperties": false,
      "properties": {
        "wire": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$",
          "description": "Snake_case wire-format category string carried on the result envelope. Must be unique across the spec. The PascalCase enum member / TS union value is derived from this string. The closed set MUST match the 'category' enum in contracts/error-codes/error-codes.canonical.schema.json byte-for-byte."
        },
        "doc": {
          "type": "string",
          "minLength": 1,
          "description": "XML <summary> / JSDoc text rendered on the emitted enum member (.NET) and union member comment (TS)."
        },
        "deprecated": {
          "type": "boolean",
          "description": "When true, the entry is deprecated: the generated C# constant + factory carry [Obsolete] and the TS constant + factory carry @deprecated. Additive — the entry is NEVER deleted. Deleting a deprecated (or non-deprecated) entry is a breaking change the JSON-diff gate fails."
        },
        "deprecatedReason": {
          "type": "string",
          "minLength": 1,
          "description": "Plain dev-facing English explaining why the code is deprecated; rendered into the [Obsolete(\"...\")] message, the @deprecated JSDoc, and the changelog entry. NOT a TK key (developer-facing, not user-facing)."
        },
        "replacedBy": {
          "type": "string",
          "description": "Wire code of the successor entry (e.g. RESOURCE_NOT_FOUND). Must reference a real, non-deprecated entry in the merged registry. Omit for pure retirement with no replacement."
        },
        "sunset": {
          "type": "string",
          "format": "date",
          "description": "ISO-8601 date for the future RFC 8594 Sunset response header. Edge-runtime-deferred: carried on the contract now, consumed by the Edge response middleware when it exists. Inert until then."
        }
      }
    }
  }
}
