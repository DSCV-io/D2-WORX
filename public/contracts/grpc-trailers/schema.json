{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "D2 gRPC trailers spec",
  "description": "Schema for contracts/grpc-trailers/grpc-trailers.spec.json — the source-of-truth catalog of gRPC trailer keys emitted on auth-pipeline RpcException failures. DcsvIo.D2.Grpc.Trailers.SourceGen reads it to emit D2GrpcTrailers into DcsvIo.D2.Auth.Grpc; tools/ts-codegen/src/grpc-trailers-emit.ts reads it to emit grpc-trailers.g.ts into @dcsv-io/d2-grpc-client. Both sides reference the spec-derived constants — wire-format drift on the trailer keys is structurally impossible.",
  "type": "object",
  "required": ["trailers"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "trailers": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/trailer" },
      "description": "Closed catalog of gRPC trailer-key constants. Each entry pins one trailer key (e.g. 'd2_error_code', 'd2_messages', 'traceId')."
    }
  },
  "definitions": {
    "trailer": {
      "type": "object",
      "required": ["constName", "value", "doc"],
      "additionalProperties": false,
      "properties": {
        "constName": {
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]*$",
          "description": "Public C# / TS constant identifier — UPPER_SNAKE_CASE. Must be unique across the spec."
        },
        "value": {
          "type": "string",
          "minLength": 1,
          "description": "Wire-format trailer key emitted into the gRPC trailer Metadata. Must be unique across the spec."
        },
        "doc": {
          "type": "string",
          "minLength": 1,
          "description": "XML <summary> / JSDoc text rendered on the emitted constant."
        },
        "deprecated": {
          "type": "boolean",
          "description": "When true, the entry is deprecated: the generated C# constant + factory carry [Obsolete] and the TS constant + factory carry @deprecated. Additive — the entry is NEVER deleted. Deleting a deprecated (or non-deprecated) entry is a breaking change the JSON-diff gate fails."
        },
        "deprecatedReason": {
          "type": "string",
          "minLength": 1,
          "description": "Plain dev-facing English explaining why the code is deprecated; rendered into the [Obsolete(\"...\")] message, the @deprecated JSDoc, and the changelog entry. NOT a TK key (developer-facing, not user-facing)."
        },
        "replacedBy": {
          "type": "string",
          "description": "Wire code of the successor entry (e.g. RESOURCE_NOT_FOUND). Must reference a real, non-deprecated entry in the merged registry. Omit for pure retirement with no replacement."
        },
        "sunset": {
          "type": "string",
          "format": "date",
          "description": "ISO-8601 date for the future RFC 8594 Sunset response header. Edge-runtime-deferred: carried on the contract now, consumed by the Edge response middleware when it exists. Inert until then."
        }
      }
    }
  }
}
