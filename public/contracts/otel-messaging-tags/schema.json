{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "D2 OTel messaging activity tags spec",
  "description": "Schema for contracts/otel-messaging-tags/otel-messaging-tags.spec.json — the source-of-truth catalog of OTel semantic-convention attribute names (and a small set of D2-specific d2.* attributes) emitted on RabbitMQ publisher + consumer spans. DcsvIo.D2.OtelMessagingTags.SourceGen reads it to emit MessagingActivityTags into DcsvIo.D2.Messaging.RabbitMq; tools/ts-codegen/src/otel-messaging-tags-emit.ts reads it to emit otel-messaging-tags.g.ts into @dcsv-io/d2-telemetry. Spec-driving the catalog structurally prevents the kind of drift bug where the consumer side wrote 'messaging.operation' while the publisher correctly wrote 'messaging.operation.type'.",
  "type": "object",
  "required": ["tags"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "tags": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/tag" },
      "description": "Closed catalog of OTel messaging activity-tag entries."
    }
  },
  "definitions": {
    "tag": {
      "type": "object",
      "required": ["constName", "value", "doc"],
      "additionalProperties": false,
      "properties": {
        "constName": {
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]*$",
          "description": "Public C# / TS constant identifier — UPPER_SNAKE_CASE. Must be unique across the spec."
        },
        "value": {
          "type": "string",
          "minLength": 1,
          "description": "Wire-format OTel attribute name emitted on the Activity. Must be unique across the spec."
        },
        "doc": {
          "type": "string",
          "minLength": 1,
          "description": "XML <summary> / JSDoc text rendered on the emitted constant."
        },
        "deprecated": {
          "type": "boolean",
          "description": "When true, the entry is deprecated: the generated C# constant + factory carry [Obsolete] and the TS constant + factory carry @deprecated. Additive — the entry is NEVER deleted. Deleting a deprecated (or non-deprecated) entry is a breaking change the JSON-diff gate fails."
        },
        "deprecatedReason": {
          "type": "string",
          "minLength": 1,
          "description": "Plain dev-facing English explaining why the code is deprecated; rendered into the [Obsolete(\"...\")] message, the @deprecated JSDoc, and the changelog entry. NOT a TK key (developer-facing, not user-facing)."
        },
        "replacedBy": {
          "type": "string",
          "description": "Wire code of the successor entry (e.g. RESOURCE_NOT_FOUND). Must reference a real, non-deprecated entry in the merged registry. Omit for pure retirement with no replacement."
        },
        "sunset": {
          "type": "string",
          "format": "date",
          "description": "ISO-8601 date for the future RFC 8594 Sunset response header. Edge-runtime-deferred: carried on the contract now, consumed by the Edge response middleware when it exists. Inert until then."
        }
      }
    }
  }
}
