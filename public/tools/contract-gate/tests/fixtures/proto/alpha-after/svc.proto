// Test fixture — alpha proto after (field 'b' deleted, no reserved).
// Package: d2.fixture.v2alpha — EXEMPT from breaking-change enforcement.
// Gate should pass (exempt) even though a field was removed without reserved.
syntax = "proto3";

package d2.fixture.v2alpha;

option csharp_namespace = "D2.Services.Protos.Fixture.V2Alpha";

message M {
  string a = 1;
}
