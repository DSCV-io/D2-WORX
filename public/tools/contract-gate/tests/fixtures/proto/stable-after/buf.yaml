# buf module config for the stable-after test fixture.
# Used by the proto-arm integration test (non-vacuity proof).
version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
