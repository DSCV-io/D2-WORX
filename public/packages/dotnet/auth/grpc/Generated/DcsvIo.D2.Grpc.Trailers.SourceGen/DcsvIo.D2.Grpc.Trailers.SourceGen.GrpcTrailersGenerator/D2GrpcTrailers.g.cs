// -----------------------------------------------------------------------
// <auto-generated>
//   Generated by DcsvIo.D2.Grpc.Trailers.SourceGen.GrpcTrailersGenerator
//   from contracts/grpc-trailers/grpc-trailers.spec.json (the source
//   of truth). Manual edits will be lost on rebuild.
// </auto-generated>
// -----------------------------------------------------------------------

#nullable enable

using System.Collections.Generic;

namespace DcsvIo.D2.Auth.Grpc.Status;

/// <summary>
/// Spec-derived gRPC trailer-key constants. Every gRPC server / client
/// references these constants for the trailer Metadata keys instead of
/// inline string literals — drift between the wire and the code is
/// structurally impossible.
/// </summary>
/// <remarks>
/// Cross-language parity: the SAME spec drives the TS-side
/// <c>@dcsv-io/d2-grpc-client</c> catalog via <c>tools/ts-codegen</c>. Both
/// sides emit the same wire keys byte-for-byte; cross-language wire
/// drift is impossible.
/// </remarks>
public static class D2GrpcTrailers
{
    /// <summary>Trailer key carrying the machine-readable d2_error_code constant. Same wire value as the HTTP ProblemDetails extension key, so operators correlating a gRPC failure with an HTTP failure see one identifier across both transports.</summary>
    public const string ERROR_CODE = "d2_error_code";

    /// <summary>Trailer key carrying the JSON-serialized D2Result.Messages array (TKMessage objects: {key, params?}). Wire-format-compatible with the HTTP ProblemDetails d2_messages extension.</summary>
    public const string MESSAGES = "d2_messages";

    /// <summary>Trailer key carrying the W3C trace id (lower-hex, 32 chars) when an Activity is current. OMITTED when no Activity is present, mirroring the HTTP middleware's omission policy. NOTE: camelCase 'traceId' value matches the HTTP ProblemDetails extension key 'traceId' — single mental model across transports. gRPC HTTP/2 trailer names are case-insensitive per the HTTP/2 spec; senders may pick any casing, and the spec pins camelCase so consumers comparing trailer values against the HTTP ProblemDetails extension key share one string literal across transports.</summary>
    public const string TRACE_ID = "traceId";

    /// <summary>
    /// All declared trailer wire-keys in spec order. Useful for
    /// cross-spec consistency tests and bulk header inspection.
    /// </summary>
    public static IReadOnlyList<string> AllTrailers => sr_allTrailers;

    private static readonly IReadOnlyList<string> sr_allTrailers = new string[]
    {
        "d2_error_code",
        "d2_messages",
        "traceId",
    };
}
