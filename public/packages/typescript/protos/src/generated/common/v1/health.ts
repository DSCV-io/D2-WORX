// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: common/v1/health.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "d2.common.v1";

export interface CheckHealthRequest {}

export interface CheckHealthResponse {
  status: string;
  timestamp: string;
  components: { [key: string]: ComponentHealth };
}

export interface CheckHealthResponse_ComponentsEntry {
  key: string;
  value?: ComponentHealth | undefined;
}

export interface ComponentHealth {
  status: string;
  latencyMs: number;
  error?: string | undefined;
}

function createBaseCheckHealthRequest(): CheckHealthRequest {
  return {};
}

export const CheckHealthRequest: MessageFns<CheckHealthRequest> = {
  encode(
    _: CheckHealthRequest,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): CheckHealthRequest {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CheckHealthRequest {
    return {};
  },

  toJSON(_: CheckHealthRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<CheckHealthRequest>): CheckHealthRequest {
    return CheckHealthRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<CheckHealthRequest>): CheckHealthRequest {
    const message = createBaseCheckHealthRequest();
    return message;
  },
};

function createBaseCheckHealthResponse(): CheckHealthResponse {
  return { status: "", timestamp: "", components: {} };
}

export const CheckHealthResponse: MessageFns<CheckHealthResponse> = {
  encode(
    message: CheckHealthResponse,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.timestamp !== "") {
      writer.uint32(18).string(message.timestamp);
    }
    globalThis.Object.entries(message.components).forEach(
      ([key, value]: [string, ComponentHealth]) => {
        CheckHealthResponse_ComponentsEntry.encode(
          { key: key as any, value },
          writer.uint32(26).fork(),
        ).join();
      },
    );
    return writer;
  },

  decode(
    input: BinaryReader | Uint8Array,
    length?: number,
  ): CheckHealthResponse {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCheckHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = CheckHealthResponse_ComponentsEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry3.value !== undefined) {
            message.components[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CheckHealthResponse {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      timestamp: isSet(object.timestamp)
        ? globalThis.String(object.timestamp)
        : "",
      components: isObject(object.components)
        ? (
            globalThis.Object.entries(object.components) as [string, any][]
          ).reduce(
            (
              acc: { [key: string]: ComponentHealth },
              [key, value]: [string, any],
            ) => {
              acc[key] = ComponentHealth.fromJSON(value);
              return acc;
            },
            {},
          )
        : {},
    };
  },

  toJSON(message: CheckHealthResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    if (message.components) {
      const entries = globalThis.Object.entries(message.components) as [
        string,
        ComponentHealth,
      ][];
      if (entries.length > 0) {
        obj.components = {};
        entries.forEach(([k, v]) => {
          obj.components[k] = ComponentHealth.toJSON(v);
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<CheckHealthResponse>): CheckHealthResponse {
    return CheckHealthResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CheckHealthResponse>): CheckHealthResponse {
    const message = createBaseCheckHealthResponse();
    message.status = object.status ?? "";
    message.timestamp = object.timestamp ?? "";
    message.components = (
      globalThis.Object.entries(object.components ?? {}) as [
        string,
        ComponentHealth,
      ][]
    ).reduce(
      (
        acc: { [key: string]: ComponentHealth },
        [key, value]: [string, ComponentHealth],
      ) => {
        if (value !== undefined) {
          acc[key] = ComponentHealth.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseCheckHealthResponse_ComponentsEntry(): CheckHealthResponse_ComponentsEntry {
  return { key: "", value: undefined };
}

export const CheckHealthResponse_ComponentsEntry: MessageFns<CheckHealthResponse_ComponentsEntry> =
  {
    encode(
      message: CheckHealthResponse_ComponentsEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== undefined) {
        ComponentHealth.encode(message.value, writer.uint32(18).fork()).join();
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): CheckHealthResponse_ComponentsEntry {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCheckHealthResponse_ComponentsEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = ComponentHealth.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): CheckHealthResponse_ComponentsEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value)
          ? ComponentHealth.fromJSON(object.value)
          : undefined,
      };
    },

    toJSON(message: CheckHealthResponse_ComponentsEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== undefined) {
        obj.value = ComponentHealth.toJSON(message.value);
      }
      return obj;
    },

    create(
      base?: DeepPartial<CheckHealthResponse_ComponentsEntry>,
    ): CheckHealthResponse_ComponentsEntry {
      return CheckHealthResponse_ComponentsEntry.fromPartial(base ?? {});
    },
    fromPartial(
      object: DeepPartial<CheckHealthResponse_ComponentsEntry>,
    ): CheckHealthResponse_ComponentsEntry {
      const message = createBaseCheckHealthResponse_ComponentsEntry();
      message.key = object.key ?? "";
      message.value =
        object.value !== undefined && object.value !== null
          ? ComponentHealth.fromPartial(object.value)
          : undefined;
      return message;
    },
  };

function createBaseComponentHealth(): ComponentHealth {
  return { status: "", latencyMs: 0, error: undefined };
}

export const ComponentHealth: MessageFns<ComponentHealth> = {
  encode(
    message: ComponentHealth,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.latencyMs !== 0) {
      writer.uint32(16).int64(message.latencyMs);
    }
    if (message.error !== undefined) {
      writer.uint32(26).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComponentHealth {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComponentHealth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.latencyMs = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComponentHealth {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      latencyMs: isSet(object.latencyMs)
        ? globalThis.Number(object.latencyMs)
        : isSet(object.latency_ms)
          ? globalThis.Number(object.latency_ms)
          : 0,
      error: isSet(object.error) ? globalThis.String(object.error) : undefined,
    };
  },

  toJSON(message: ComponentHealth): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.latencyMs !== 0) {
      obj.latencyMs = Math.round(message.latencyMs);
    }
    if (message.error !== undefined) {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<ComponentHealth>): ComponentHealth {
    return ComponentHealth.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ComponentHealth>): ComponentHealth {
    const message = createBaseComponentHealth();
    message.status = object.status ?? "";
    message.latencyMs = object.latencyMs ?? 0;
    message.error = object.error ?? undefined;
    return message;
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends { $case: string }
        ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & {
            $case: T["$case"];
          }
        : T extends {}
          ? { [K in keyof T]?: DeepPartial<T[K]> }
          : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
