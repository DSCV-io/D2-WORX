// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               unknown
// source: common/v1/d2_result.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "d2.common.v1";

/**
 * A translatable message: a translation key plus an optional dictionary of
 * substitution parameters. Mirrors TKMessage (DcsvIo.D2.I18n / @dcsv-io/d2-result).
 * params values are always strings on both runtimes.
 */
export interface TKMessageProto {
  key: string;
  params: { [key: string]: string };
}

export interface TKMessageProto_ParamsEntry {
  key: string;
  value: string;
}

export interface D2ResultProto {
  success: boolean;
  statusCode: number;
  errorCode?: string | undefined;
  /**
   * snake_case ErrorCategory wire string (e.g. "not_found", "conflict").
   * Absent (proto3 optional) when the result has no category.
   */
  category?: string | undefined;
  traceId?: string | undefined;
  messages: TKMessageProto[];
  inputErrors: InputErrorProto[];
}

export interface InputErrorProto {
  field: string;
  errors: TKMessageProto[];
}

function createBaseTKMessageProto(): TKMessageProto {
  return { key: "", params: {} };
}

export const TKMessageProto: MessageFns<TKMessageProto> = {
  encode(
    message: TKMessageProto,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    globalThis.Object.entries(message.params).forEach(
      ([key, value]: [string, string]) => {
        TKMessageProto_ParamsEntry.encode(
          { key: key as any, value },
          writer.uint32(18).fork(),
        ).join();
      },
    );
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TKMessageProto {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTKMessageProto();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = TKMessageProto_ParamsEntry.decode(
            reader,
            reader.uint32(),
          );
          if (entry2.value !== undefined) {
            message.params[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TKMessageProto {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      params: isObject(object.params)
        ? (globalThis.Object.entries(object.params) as [string, any][]).reduce(
            (acc: { [key: string]: string }, [key, value]: [string, any]) => {
              acc[key] = globalThis.String(value);
              return acc;
            },
            {},
          )
        : {},
    };
  },

  toJSON(message: TKMessageProto): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.params) {
      const entries = globalThis.Object.entries(message.params) as [
        string,
        string,
      ][];
      if (entries.length > 0) {
        obj.params = {};
        entries.forEach(([k, v]) => {
          obj.params[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<TKMessageProto>): TKMessageProto {
    return TKMessageProto.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TKMessageProto>): TKMessageProto {
    const message = createBaseTKMessageProto();
    message.key = object.key ?? "";
    message.params = (
      globalThis.Object.entries(object.params ?? {}) as [string, string][]
    ).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseTKMessageProto_ParamsEntry(): TKMessageProto_ParamsEntry {
  return { key: "", value: "" };
}

export const TKMessageProto_ParamsEntry: MessageFns<TKMessageProto_ParamsEntry> =
  {
    encode(
      message: TKMessageProto_ParamsEntry,
      writer: BinaryWriter = new BinaryWriter(),
    ): BinaryWriter {
      if (message.key !== "") {
        writer.uint32(10).string(message.key);
      }
      if (message.value !== "") {
        writer.uint32(18).string(message.value);
      }
      return writer;
    },

    decode(
      input: BinaryReader | Uint8Array,
      length?: number,
    ): TKMessageProto_ParamsEntry {
      const reader =
        input instanceof BinaryReader ? input : new BinaryReader(input);
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTKMessageProto_ParamsEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    },

    fromJSON(object: any): TKMessageProto_ParamsEntry {
      return {
        key: isSet(object.key) ? globalThis.String(object.key) : "",
        value: isSet(object.value) ? globalThis.String(object.value) : "",
      };
    },

    toJSON(message: TKMessageProto_ParamsEntry): unknown {
      const obj: any = {};
      if (message.key !== "") {
        obj.key = message.key;
      }
      if (message.value !== "") {
        obj.value = message.value;
      }
      return obj;
    },

    create(
      base?: DeepPartial<TKMessageProto_ParamsEntry>,
    ): TKMessageProto_ParamsEntry {
      return TKMessageProto_ParamsEntry.fromPartial(base ?? {});
    },
    fromPartial(
      object: DeepPartial<TKMessageProto_ParamsEntry>,
    ): TKMessageProto_ParamsEntry {
      const message = createBaseTKMessageProto_ParamsEntry();
      message.key = object.key ?? "";
      message.value = object.value ?? "";
      return message;
    },
  };

function createBaseD2ResultProto(): D2ResultProto {
  return {
    success: false,
    statusCode: 0,
    errorCode: undefined,
    category: undefined,
    traceId: undefined,
    messages: [],
    inputErrors: [],
  };
}

export const D2ResultProto: MessageFns<D2ResultProto> = {
  encode(
    message: D2ResultProto,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.statusCode !== 0) {
      writer.uint32(16).int32(message.statusCode);
    }
    if (message.errorCode !== undefined) {
      writer.uint32(26).string(message.errorCode);
    }
    if (message.category !== undefined) {
      writer.uint32(34).string(message.category);
    }
    if (message.traceId !== undefined) {
      writer.uint32(42).string(message.traceId);
    }
    for (const v of message.messages) {
      TKMessageProto.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.inputErrors) {
      InputErrorProto.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): D2ResultProto {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseD2ResultProto();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.statusCode = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errorCode = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.category = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.traceId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.messages.push(TKMessageProto.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.inputErrors.push(
            InputErrorProto.decode(reader, reader.uint32()),
          );
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): D2ResultProto {
    return {
      success: isSet(object.success)
        ? globalThis.Boolean(object.success)
        : false,
      statusCode: isSet(object.statusCode)
        ? globalThis.Number(object.statusCode)
        : isSet(object.status_code)
          ? globalThis.Number(object.status_code)
          : 0,
      errorCode: isSet(object.errorCode)
        ? globalThis.String(object.errorCode)
        : isSet(object.error_code)
          ? globalThis.String(object.error_code)
          : undefined,
      category: isSet(object.category)
        ? globalThis.String(object.category)
        : undefined,
      traceId: isSet(object.traceId)
        ? globalThis.String(object.traceId)
        : isSet(object.trace_id)
          ? globalThis.String(object.trace_id)
          : undefined,
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => TKMessageProto.fromJSON(e))
        : [],
      inputErrors: globalThis.Array.isArray(object?.inputErrors)
        ? object.inputErrors.map((e: any) => InputErrorProto.fromJSON(e))
        : globalThis.Array.isArray(object?.input_errors)
          ? object.input_errors.map((e: any) => InputErrorProto.fromJSON(e))
          : [],
    };
  },

  toJSON(message: D2ResultProto): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.statusCode !== 0) {
      obj.statusCode = Math.round(message.statusCode);
    }
    if (message.errorCode !== undefined) {
      obj.errorCode = message.errorCode;
    }
    if (message.category !== undefined) {
      obj.category = message.category;
    }
    if (message.traceId !== undefined) {
      obj.traceId = message.traceId;
    }
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => TKMessageProto.toJSON(e));
    }
    if (message.inputErrors?.length) {
      obj.inputErrors = message.inputErrors.map((e) =>
        InputErrorProto.toJSON(e),
      );
    }
    return obj;
  },

  create(base?: DeepPartial<D2ResultProto>): D2ResultProto {
    return D2ResultProto.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<D2ResultProto>): D2ResultProto {
    const message = createBaseD2ResultProto();
    message.success = object.success ?? false;
    message.statusCode = object.statusCode ?? 0;
    message.errorCode = object.errorCode ?? undefined;
    message.category = object.category ?? undefined;
    message.traceId = object.traceId ?? undefined;
    message.messages =
      object.messages?.map((e) => TKMessageProto.fromPartial(e)) || [];
    message.inputErrors =
      object.inputErrors?.map((e) => InputErrorProto.fromPartial(e)) || [];
    return message;
  },
};

function createBaseInputErrorProto(): InputErrorProto {
  return { field: "", errors: [] };
}

export const InputErrorProto: MessageFns<InputErrorProto> = {
  encode(
    message: InputErrorProto,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.field !== "") {
      writer.uint32(10).string(message.field);
    }
    for (const v of message.errors) {
      TKMessageProto.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InputErrorProto {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInputErrorProto();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.field = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.errors.push(TKMessageProto.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InputErrorProto {
    return {
      field: isSet(object.field) ? globalThis.String(object.field) : "",
      errors: globalThis.Array.isArray(object?.errors)
        ? object.errors.map((e: any) => TKMessageProto.fromJSON(e))
        : [],
    };
  },

  toJSON(message: InputErrorProto): unknown {
    const obj: any = {};
    if (message.field !== "") {
      obj.field = message.field;
    }
    if (message.errors?.length) {
      obj.errors = message.errors.map((e) => TKMessageProto.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<InputErrorProto>): InputErrorProto {
    return InputErrorProto.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<InputErrorProto>): InputErrorProto {
    const message = createBaseInputErrorProto();
    message.field = object.field ?? "";
    message.errors =
      object.errors?.map((e) => TKMessageProto.fromPartial(e)) || [];
    return message;
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends { $case: string }
        ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & {
            $case: T["$case"];
          }
        : T extends {}
          ? { [K in keyof T]?: DeepPartial<T[K]> }
          : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
