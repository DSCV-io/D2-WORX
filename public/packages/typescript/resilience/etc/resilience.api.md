## API Report File for "@dcsv-io/d2-resilience"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { D2Result } from '@dcsv-io/d2-result';

// @public
export class CircuitBreaker<T> {
    constructor(opts: CircuitBreakerOptions<T>);
    get currentState(): CircuitState;
    execute(op: () => Promise<T>, fallback?: () => T | Promise<T>): Promise<T>;
    reset(): void;
}

// @public
export interface CircuitBreakerOptions<T = unknown> {
    readonly cooldownMs: number;
    readonly failureThreshold: number;
    readonly isFailure?: (result: T) => boolean;
    readonly nowFunc?: () => number;
    readonly onStateChange?: (from: CircuitState, to: CircuitState) => void;
}

// @public
export class CircuitOpenError extends Error {
    constructor(message?: string);
}

// @public
export const CircuitState: {
    readonly Closed: "closed";
    readonly Open: "open";
    readonly HalfOpen: "half-open";
};

// @public (undocumented)
export type CircuitState = (typeof CircuitState)[keyof typeof CircuitState];

// @public
export function defaultIsTransient(err: unknown): boolean;

// @public
export interface IResilientLayer {
    // (undocumented)
    execute<T>(key: string, op: (signal?: AbortSignal) => Promise<T>, signal?: AbortSignal): Promise<T>;
}

// @public
export const RATE_LIMITER_DEFAULTS: RateLimiterOptions;

// @public
export class RateLimiterLayer implements IResilientLayer {
    constructor(inner: IResilientLayer, opts?: RateLimiterOptions);
    get activeCount(): number;
    // (undocumented)
    execute<T>(key: string, op: (signal?: AbortSignal) => Promise<T>, signal?: AbortSignal): Promise<T>;
}

// @public
export interface RateLimiterOptions {
    readonly acquisitionTimeoutMs: number;
    readonly maxConcurrency: number;
}

// @public
export class RateLimitRejectedError extends Error {
    constructor(message?: string);
}

// @public
export class ResilientPipeline {
    constructor(head: IResilientLayer);
    execute<T>(key: string, op: (signal?: AbortSignal) => Promise<T>, signal?: AbortSignal): Promise<T>;
    static get PassThrough(): ResilientPipeline;
}

// @public
export class ResilientPipelineBuilder {
    // (undocumented)
    build(): ResilientPipeline;
    // (undocumented)
    useCircuitBreaker(opts: CircuitBreakerOptions): this;
    useRateLimiter(opts?: RateLimiterOptions): this;
    // (undocumented)
    useRetries(opts: Partial<RetryOptions<unknown>>): this;
    // (undocumented)
    useSingleflight(): this;
    useTimeout(opts?: TimeoutOptions): this;
}

// @public
export const RETRY_DEFAULTS: RetryOptions<unknown>;

// @public
export const RetryHelper: {
    readonly retryAsync: <T>(op: (attempt: number) => Promise<T>, opts?: Partial<RetryOptions<T>>, signal?: AbortSignal, rng?: () => number) => Promise<T>;
    readonly retryD2ResultAsync: <T>(op: (attempt: number) => Promise<D2Result<T>>, opts?: Partial<RetryOptions<D2Result<T>>>, signal?: AbortSignal, rng?: () => number) => Promise<D2Result<T>>;
};

// @public
export interface RetryOptions<T = unknown> {
    readonly backoffMultiplier: number;
    readonly baseDelayMs: number;
    readonly delayFunc?: (ms: number, signal?: AbortSignal) => Promise<void>;
    readonly isTransient?: (err: unknown) => boolean;
    readonly jitter: number;
    readonly maxAttempts: number;
    readonly maxDelayMs: number;
    readonly shouldRetry?: (errOrValue: T | unknown) => boolean;
}

// @public
export class Singleflight<TKey, TValue> {
    do(key: TKey, factory: () => Promise<TValue>): Promise<TValue>;
    get inflightCount(): number;
}

// @public
export const TIMEOUT_DEFAULTS: TimeoutOptions;

// @public
export class TimeoutError extends Error {
    constructor(message?: string);
}

// @public
export class TimeoutLayer implements IResilientLayer {
    constructor(inner: IResilientLayer, opts?: TimeoutOptions);
    // (undocumented)
    execute<T>(key: string, op: (signal?: AbortSignal) => Promise<T>, signal?: AbortSignal): Promise<T>;
}

// @public
export interface TimeoutOptions {
    readonly durationMs: number;
}

// (No @packageDocumentation comment for this package)

```
