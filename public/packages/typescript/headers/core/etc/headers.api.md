## API Report File for "@d2/headers"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { D2Result } from '@d2/result';
import { defaultTitleForStatus } from '@d2/problem-details-abstractions';
import type { IRequestContext } from '@d2/request-context-abstractions';
import { JwtPayload } from '@d2/auth-abstractions';
import type { OrgType } from '@d2/auth-context-abstractions';
import { PROBLEM_DETAILS_CONTENT_TYPE } from '@d2/problem-details-abstractions';
import { PROBLEM_TYPE_URI_PREFIX } from '@d2/problem-details-abstractions';
import { ProblemDetailsExtensionKeys } from '@d2/problem-details-abstractions';
import { ProblemDetailsTitles } from '@d2/problem-details-abstractions';
import type { Role } from '@d2/auth-context-abstractions';

// @public
export type AuthenticatedRequestContext = IRequestContext & {
    readonly isAuthenticated: true;
};

export { defaultTitleForStatus }

// @public
export interface GuardRequestEvent {
    // (undocumented)
    locals: {
        requestContext?: IRequestContext;
    };
    // (undocumented)
    readonly url: {
        readonly pathname: string;
    };
}

// @public
export interface GuardThrowers {
    throwError(status: number, body: ProblemDetailsBody, contentType: string): never;
    throwRedirect(status: number, location: string): never;
}

export { JwtPayload }

// @public
export function parseAuthHeader(authHeader: string | null | undefined, opts?: ParseAuthHeaderOptions): D2Result<JwtPayload>;

// @public
export interface ParseAuthHeaderOptions {
    readonly traceId?: string;
}

// @public
export function parseRequestContextFromHeaders(headers: Headers, opts?: ParseRequestContextOptions): D2Result<IRequestContext>;

// @public
export interface ParseRequestContextOptions {
    readonly requireAuth?: boolean;
    readonly traceId?: string;
}

export { PROBLEM_DETAILS_CONTENT_TYPE }

export { PROBLEM_TYPE_URI_PREFIX }

// @public
export interface ProblemDetailsBody {
    // (undocumented)
    readonly [key: string]: unknown;
    // (undocumented)
    readonly detail?: string;
    // (undocumented)
    readonly instance: string;
    // (undocumented)
    readonly status: number;
    // (undocumented)
    readonly title: string;
    // (undocumented)
    readonly type: string;
}

export { ProblemDetailsExtensionKeys }

// @public
export interface ProblemDetailsOptions {
    readonly detail?: string;
    readonly instance: string;
    readonly title?: string;
}

export { ProblemDetailsTitles }

// @public
export function redirectIfAuthenticated(event: GuardRequestEvent, throwers: GuardThrowers, to: string): void;

// @public
export function requireAuth(event: GuardRequestEvent, throwers: GuardThrowers): asserts event is GuardRequestEvent & {
    locals: {
        requestContext: AuthenticatedRequestContext;
    };
};

// @public
export function requireOrg(event: GuardRequestEvent, throwers: GuardThrowers, ...types: readonly OrgType[]): asserts event is GuardRequestEvent & {
    locals: {
        requestContext: AuthenticatedRequestContext;
    };
};

// @public
export function requireRole(event: GuardRequestEvent, throwers: GuardThrowers, ...roles: readonly Role[]): asserts event is GuardRequestEvent & {
    locals: {
        requestContext: AuthenticatedRequestContext;
    };
};

// @public
export function requireScope(event: GuardRequestEvent, throwers: GuardThrowers, ...scopes: readonly string[]): asserts event is GuardRequestEvent & {
    locals: {
        requestContext: AuthenticatedRequestContext;
    };
};

// @public
export function toProblemDetails(failure: D2Result<unknown>, opts: ProblemDetailsOptions): ProblemDetailsBody;

// (No @packageDocumentation comment for this package)

```
