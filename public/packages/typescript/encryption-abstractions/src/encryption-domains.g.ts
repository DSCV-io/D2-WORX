// -----------------------------------------------------------------------
// <auto-generated>
//   Generated by tools/ts-codegen — see this script's source for details.
//   Source spec: contracts/encryption-domains/encryption-domains.spec.json
//   Manual edits will be lost on rebuild.
// </auto-generated>
// -----------------------------------------------------------------------

/* eslint-disable */

/**
 * Spec-derived closed catalog of encryption-domain identifiers.
 * Mirrors .NET DcsvIo.D2.Encryption.EncryptionDomains (same wire values).
 *
 * Use these constants instead of raw strings so a typo can't silently
 * route a message to a non-existent keyring. The PLAINTEXT sentinel is
 * included so callers can distinguish 'no encryption' from a real domain.
 */
export const EncryptionDomains = {
  /**
   * Sentinel value indicating the message body MUST NOT be encrypted (always serialized as cleartext JSON). Surfaces on mq-messages.spec entries whose payload is non-PII; the message author MUST also document `encryptionReason` on the spec entry justifying the choice.
   */
  PLAINTEXT: "plaintext",
  /**
   * Public framework fixture domain for Shared.Tests sealed-path unit coverage of EncryptionDomainModes + EncryptedBodyComposer. Not a product keyring domain — product sealed domains (audit / notifications / courier) live in private/contracts/encryption-domains.
   */
  FIXTURE_SEALED: "payload-fixture-sealed",
} as const;

export type EncryptionDomain =
  (typeof EncryptionDomains)[keyof typeof EncryptionDomains];

export const ALL_ENCRYPTION_DOMAINS: readonly string[] = [
  "plaintext",
  "payload-fixture-sealed",
];

/**
 * Per-domain payload encryption mode, keyed by wire value. `symmetric` =
 * shared-keyring AES-256-GCM (v1 frame); `sealed` = per-consumer-service
 * ECDH (v2 frame). Literal-typed for the publisher type-witness. Mirrors
 * .NET EncryptionDomainModes.
 */
export const EncryptionDomainModes = {
  "plaintext": "symmetric",
  "payload-fixture-sealed": "sealed",
} as const;

export type EncryptionDomainMode = "symmetric" | "sealed";

/**
 * Consumer ServiceId per SEALED domain (the single decryptor). Only
 * sealed domains appear. Mirrors .NET
 * EncryptionDomainModes.ConsumerServiceByDomain.
 */
export const ConsumerServiceByDomain = {
  "payload-fixture-sealed": "payload-fixture-sealed",
} as const;
