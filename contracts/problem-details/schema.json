{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "D2 ProblemDetails spec",
  "description": "Schema for contracts/problem-details/problem-details.spec.json — the source-of-truth catalog that D2.Shared.ProblemDetails.SourceGen reads to emit the D2ProblemDetailsKeys static class into D2.Shared.ProblemDetails.Abstractions (consumed by D2.Shared.Auth.Http and D2.Shared.AspNetCore), and tools/ts-codegen/src/problem-details-emit.ts reads to emit problem-details.g.ts into @d2/problem-details-abstractions (re-exported from @d2/headers).",
  "type": "object",
  "required": ["typeUriPrefix", "contentType", "extensionKeys", "titles"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "typeUriPrefix": {
      "type": "string",
      "format": "uri",
      "description": "Base URI for the RFC 7807 ProblemDetails 'type' field. The runtime appends the kebab-cased error code to this prefix (e.g. 'https://problems.d2.dcsv.io/' + 'auth-bearer-missing'). Must end with a trailing slash."
    },
    "contentType": {
      "type": "string",
      "minLength": 1,
      "description": "MIME type emitted on responses carrying an RFC 7807 ProblemDetails body. Per RFC 7807 §6.1, the IANA-registered value is 'application/problem+json' for JSON bodies. Single source of truth so .NET middleware (JwtAuthMiddleware) and TS BFF guards write byte-identical Content-Type headers."
    },
    "extensionKeys": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/extensionKey" },
      "description": "Closed catalog of extension keys emitted on the RFC 7807 ProblemDetails body. Includes both D2-private d2_-namespaced keys (d2_error_code, d2_messages, d2_input_errors) and unprefixed keys aligned with external conventions (traceId per W3C-OTel; correlationId per HTTP X-Correlation-Id header)."
    },
    "titles": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/title" },
      "description": "Closed catalog of human-readable 'title' values keyed by HTTP status. Exactly one entry MAY carry httpStatus=null as the fallback title used when no httpStatus row matches."
    }
  },
  "definitions": {
    "extensionKey": {
      "type": "object",
      "required": ["constName", "value", "doc"],
      "additionalProperties": false,
      "properties": {
        "constName": {
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]*$",
          "description": "Public C# / TS constant identifier — UPPER_SNAKE_CASE. Must be unique across the spec."
        },
        "value": {
          "type": "string",
          "minLength": 1,
          "description": "Wire-format extension key emitted on the RFC 7807 ProblemDetails JSON body (e.g. 'd2_error_code', 'traceId'). Must be unique across the spec."
        },
        "doc": {
          "type": "string",
          "minLength": 1,
          "description": "XML <summary> / JSDoc text rendered on the emitted constant."
        }
      }
    },
    "title": {
      "type": "object",
      "required": ["constName", "httpStatus", "value", "doc"],
      "additionalProperties": false,
      "properties": {
        "constName": {
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]*$",
          "description": "Public C# / TS constant identifier — UPPER_SNAKE_CASE. Must be unique across the spec."
        },
        "httpStatus": {
          "type": ["integer", "null"],
          "minimum": 100,
          "maximum": 599,
          "description": "HTTP status this title maps to (e.g. 401, 503). null marks the fallback title used when no httpStatus row matches. At most one entry MAY carry httpStatus=null."
        },
        "value": {
          "type": "string",
          "minLength": 1,
          "description": "Wire-format title value emitted on the RFC 7807 ProblemDetails JSON body (e.g. 'Unauthorized', 'Service Unavailable'). Locale-NEUTRAL English from a closed enumeration — locale-aware translation is the client's job via the d2_messages extension. Must be unique across the spec."
        },
        "doc": {
          "type": "string",
          "minLength": 1,
          "description": "XML <summary> / JSDoc text rendered on the emitted constant."
        }
      }
    }
  }
}
