{
  "$schema": "./schema.json",
  "properties": [
    {
      "constName": "KEY",
      "value": "key",
      "doc": "JSON property name carrying the translation key (e.g. 'common_errors_NOT_FOUND'). REQUIRED on every TKMessage; absent payloads are rejected by both the .NET TKMessageJsonConverter and the TS-side parser."
    },
    {
      "constName": "PARAMS",
      "value": "params",
      "doc": "JSON property name carrying the optional parameter dictionary (Record<string, unknown> of placeholder name -> substitution value). OMITTED when no parameters are bound; present as a JSON object when one-or-more bindings exist."
    }
  ]
}
