{
  "$schema": "./schema.json",
  "domains": [
    {
      "constName": "AUDIT",
      "value": "audit",
      "mode": "sealed",
      "consumerService": "audit",
      "doc": "Audit events. All services publish; D2.Audit consumes. Carries actor + actee identities, action descriptors, IPs, and fingerprints — fully PII-bearing on every event. SEALED: every service seals to the audit service's public key; only D2.Audit opens."
    },
    {
      "constName": "NOTIFICATIONS",
      "value": "notifications",
      "mode": "sealed",
      "consumerService": "notifications",
      "doc": "Notification requests (the input shape D2.Notifications consumes). Carries recipient identity + subject / body markdown, often with names, addresses, financial figures, and verification codes. SEALED: every service seals to the notifications service's public key; only D2.Notifications opens."
    },
    {
      "constName": "COURIER",
      "value": "courier",
      "mode": "sealed",
      "consumerService": "courier",
      "doc": "Courier delivery records (the materialized email / SMS payloads D2.Notifications hands to D2.Courier). Carries fully-rendered message bodies and recipient addresses. SEALED: every service seals to the courier service's public key; only D2.Courier opens."
    },
    {
      "constName": "PLAINTEXT",
      "value": "plaintext",
      "doc": "Sentinel value indicating the message body MUST NOT be encrypted (always serialized as cleartext JSON). Surfaces on mq-messages.spec entries whose payload is non-PII; the message author MUST also document `encryptionReason` on the spec entry justifying the choice."
    }
  ]
}
