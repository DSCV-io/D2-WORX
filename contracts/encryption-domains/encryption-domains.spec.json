{
  "$schema": "./schema.json",
  "domains": [
    {
      "constName": "AUDIT",
      "value": "audit",
      "doc": "Audit events. All services publish; D2.Audit consumes. Carries actor + actee identities, action descriptors, IPs, and fingerprints — fully PII-bearing on every event."
    },
    {
      "constName": "NOTIFICATIONS",
      "value": "notifications",
      "doc": "Notification requests (the input shape D2.Notifications consumes). Carries recipient identity + subject / body markdown, often with names, addresses, financial figures, and verification codes."
    },
    {
      "constName": "COURIER",
      "value": "courier",
      "doc": "Courier delivery records (the materialized email / SMS payloads D2.Notifications hands to D2.Courier). Carries fully-rendered message bodies and recipient addresses."
    },
    {
      "constName": "PLAINTEXT",
      "value": "plaintext",
      "doc": "Sentinel value indicating the message body MUST NOT be encrypted (always serialized as cleartext JSON). Surfaces on mq-messages.spec entries whose payload is non-PII; the message author MUST also document `encryptionReason` on the spec entry justifying the choice."
    }
  ]
}
